# ===== Rust =====
/target
**/*.rs.bk
Cargo.lock
# 注：二进制/库工程通常提交 Cargo.lock；若后续定为库可改回忽略。先提交。

# ===== Unity =====
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/
[Mm]emoryCaptures/
[Rr]ecordings/
# Unity 生成的 meta 保留（资产需要）；以下为不应入库的产物
*.unitypackage
*.apk
*.aab
*.unityproj
*.sln
*.slnx
*.csproj
*.user
*.pidb
*.booproj
*.svd
*.mdb
*.opendb
*.VC.db
*.VC.opendb
sysinfo.txt
crashlytics-build.properties

# ===== dotnet tests =====
tests/dotnet/bin/
tests/dotnet/obj/
!tests/dotnet/*.csproj

# ===== Rust↔Unity 产物 =====
# csbindgen 生成的 C# 绑定（由 Rust 侧生成，不入库）
**/LoomGUI*Bindings*.cs
**/LoomGUI*Bindings*.cs.meta
!**/Plugins/LoomGUI/Bindings/LoomGUIBindings.cs
!**/Plugins/LoomGUI/Bindings/LoomGUIBindings.cs.meta
# native 库产物（按需，若想入库 prebuilt 可注释掉）
/**/*.dll
/**/*.dylib
/**/*.so
!**/Plugins/**/*.dll
!**/Plugins/**/*.dylib
!**/Plugins/**/*.so

# ===== IDE / 编辑器 =====
.vs/
.vscode/
.idea/
*.swp
*~

# ===== OS =====
.DS_Store
Thumbs.db
desktop.ini

# ===== Claude / 工具缓存 =====
.claude/cache/
.claude/state/
.claude/projects/
.claude/worktrees/

# ===== open-design 调研 =====
# open-design 源码（只读调研参考，不入库）
/temp/

# editor init 注入生成物（samples/ 自测注入）
samples/CLAUDE.md
samples/.claude/
samples/*/*.artifact.json
samples/.od-skills

# Tauri build artifacts (regenerated by tauri-build)
crates/packer/gui/src-tauri/gen/

# superpowers SDD scratch (briefs/reports/review diffs — session-local, not source)
.superpowers/
*.snap.new

# HeadlessTests fixture packer output (loom-pkg build of *.workspace → ../<name>-ws-out/)
tests/dotnet/LoomGUI.HeadlessTests/fixtures/*-ws-out/
node_modules
tmp

# SDD worktrees (local isolation)
.worktrees/
