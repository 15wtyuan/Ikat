# LoomGUI 验收清单（2026-08-14 晚 · 家里 Unity 机）

> 覆盖近几轮全部入库工作：**组件系统 8 commit**（`0606d516..0c7047d3`）+ **FFI 批四件/Touchable/Visibility 砍除** + **rect-diff --scene 模式** + **CI dotnet 门**。
> 编码机侧已全绿（cargo 1567 / dotnet Headless 383 + 纯 managed 31 / PublicApi 编译门 / fmt / clippy 严门；dll、bindings、GUI exe、showcase pkg、全部 fixture v35 已同步入库）——本清单只列真机侧。

---

## 0. 预检（开 Unity 前，手机/浏览器即可）

- [ ] **CI 首跑观察**：GitHub Actions 新 job `dotnet-headless`（HeadlessTests 现场编 Linux .so 跑 P/Invoke 全套）+ `dotnet-publicapi`（-warnaserror 编译门）。此前 CI 只跑 31 个纯 managed 测试，HeadlessTests 曾静默腐烂无人知——这次是首跑。
- [ ] **版本提示**：所有 pkg 已升 v35。若 Unity 报 `package formatVersion 34 too old` = 某处用了旧 dll 或旧包，不是新 bug。

## 1. 编译门（一次）

- [ ] Unity 打开编译零错误。本轮新增/改动的 C# 表面：`CustomElement.Tag`、`Node.Touchable`、`UIContext.Pick`、`ProgressBar.IsIndeterminate`、`NumberField.Min/Max/Step`、`RadioButton.Name`、Query 边界剪枝（`IsLookupScopeBoundary`）、ShowcaseRunner 两跳路径。
- [ ] Console 无 **GRADIENT shader** 编译错误——任务 3 的 per-fragment 渐变 shader 是纸面设计，这是它第一次真机编译（program 6/7 变体）。

## 2. 组件系统（本轮主体 · 6 个变更页）

每页流程：PlayMode 跑通 → 视觉对照浏览器预览（直接双击 HTML）→ 行为检查。

- [ ] **顶栏组件 page-top**（mail / inventory / shop / character / form / lab 六页）：顶栏正常渲染（返回按钮 + 标题），点「← 首页」回 home——这走的是新两跳路径 `page.Get<CustomElement>("page-top").Get<Button>("back-home")`，编译过 + 点击生效 = L3 边界 + 投影 + FFI 全链通。
- [ ] **character 页 stat-bar**：HP / MP / EXP 三条属性条渲染正确（投影进来的 progressbar 吃组件 scoped 样式）；点「训练 +EXP」→ EXP 条 +10、百分比文本刷新（`exp-bar` 两跳）。
- [ ] **lab 页第 10 区 item-card**：左卡 = 三 slot 全投影（宝石图标 /「宝石残片」/ 描述），右卡 = 全 fallback（默认宝箱图 /「默认名」/「默认描述」）。
- [ ] **L3 边界行为抽查**（可选，Console 验证）：`page.TryGet<Button>("back-home")` 现在返回 **false** 是预期（id 在组件域内）——契约 public-api §3.1 已更新，driver 一律先 Get 作用域根再进。
- [ ] **GUI 打包器**（`LoomGUI > Open Packer`）重打 showcase：components/ 目录被识别、9 组件打包成功（exe 已重出同步，坑 158 闭环）。
- [ ] **rect-diff `--scene` 首跑**：PlayMode 运行态导出 → normalize → 与浏览器基线 diff。工作流见 `showcase/scripts/rect-diff/snapshot-2026-08-14-8pages.md` 末节（编码机已过 round-trip 自测）。

## 3. FFI 批四件 + Touchable（上轮遗留，已验收则跳过）

- [ ] `node.Touchable = false` → 本节点不吃点击、子节点照常（透传语义）；恢复后命中回归。
- [ ] `ui.Pick(x, y)`：命中返对应 typed 节点，miss 返 null。
- [ ] `progressBar.IsIndeterminate` 切换视觉。
- [ ] `numberField.Min/Max/Step` 运行时改界后 value 文本重约束（clamp + step 量化，非数字文本不动）。
- [ ] `radioButton.Name` 读组名。
- [ ] `NodeStyle.Visibility` 不存在了 = 预期（契约砍除，`opacity:0` 替代）。

## 4. 渐变视觉验收（里程碑 1 任务 3 真机半场）

- [ ] lab 第 12 区渐变标本矩阵 vs 入库 Chrome 基准截图（`showcase/scripts/gradient-baseline/` 3 张）。
- [ ] home radial 光晕（1100×560 椭圆 @ 82% -12%，0.1 → transparent@0.6）。
- [ ] 渐变文字（CPU 采样，与 shader 同一套 t 数学）。
- [ ] 若颜色「发白/偏亮」：先按坑 197 走 sRGB vs Linear 取证流程（Chrome headless 截图 + 逐像素对比），别盲改 shader。

## 5. 里程碑 1 任务 4 / 5 债（独立可插）

- [ ] 8 页 PlayMode 全跑通 + `--scene` 逐页比对（任务 4 真机半场；静态页 → 虚拟列表页 → home 的顺序）。
- [ ] 四门验收债：ListView / 动画 / TabList / Dropdown（验收页 + pkg + checklist 已备，任务 5）。
- [ ] lab / home 容差定标（A 类文本度量精度差、C 类 slider thumb transform 发射缺口——见 `snapshot-2026-08-14-8pages.md` 四类残余表）。

---

## 已在编码机验证（勿重复）

- rect-diff：6 个组件变更页 + 此前 8 页基线全部 **0 unmatched / 0 idless-unpaired**，零疑似 core 布局 bug；残余 diff 全归既有文本度量类别。
- 组件展开打包错误语义全集（未注册 hyphen / 无效 slot / 页面裸 slot / 展开环 / 同域 id 撞车 / keyframes 同名宿主胜）有单测覆盖。
- HeadlessTests 383 全绿走的是真 P/Invoke 链路（component fixture 从打包器产 pkg → instantiate → 断言硬墙语义）。

## 相关 commit 索引

| 轮次 | 内容 | commit |
|---|---|---|
| 组件系统 | spec | `0606d516` |
| | pkg v35 core 地基（锚定规则 + HOST_IN_PARENT_SCOPE） | `86037bda` |
| | L3 查找边界 + aria-controls 多实例安全 | `a2d14d0f` |
| | 打包器展开（注册表 + slot 投影 + 校验全集） | `d34ac419` |
| | tag 选择器匹配 + fence/AGENTS 契约同步 | `7f00a38a` |
| | CustomElement.Tag + headless fixture | `466b8459` |
| | rect-diff 组件感知工具链 | `004ccb20` |
| | showcase 落地（page-top ×6 + stat-bar + item-card） | `0c7047d3` |
| 上轮 | FFI 批四件（Pick/IsIndeterminate/bounds/Radio.Name） | `d69474fe` |
| | CI dotnet 门（headless .so + PublicApi） | `8117206e` |
| | rect-diff --scene 模式 | `e9a8e851` |
| | Visibility 契约砍除 | `619cdabc` |
| | Node.Touchable | `7b95a653` |
