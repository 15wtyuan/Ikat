[package]
name = "ikat_ffi_c"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
ikat_core = { path = "../core" }
# FlexDirection 是 ComputedNodeStyle 字段类型，FFI 稳定化时须 match 其变体（见 lib.rs）。
taffy = "0.12"

[build-dependencies]
csbindgen = "1"
