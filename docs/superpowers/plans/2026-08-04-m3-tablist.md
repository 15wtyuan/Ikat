# M3 · TabList 复合控件 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 WAI-ARIA `role=tablist/tab/tabpanel` 做成框架自带复合控件，镜像已有 Dropdown 全套机制，让 settings 页 tab 高亮 + 面板切换在写标准 HTML 的前提下「写了就自动切」。

**Architecture:** α 路线（框架控件，非通用 attrs 仓库）。镜像 Dropdown：`NodeKind::TabList/Tab` + `ControlState::TabList{selected_index}` + `synth_aria_value` 跨节点合成 aria-selected + `sync_control_visuals` 用 `set_inline_override` 切 panel display（复用 display:none 剪枝）+ click/方向键改 selected_index + 复用 `EVT_SELECTION_CHANGED`。panel 跨树关联靠 `aria-controls`（存 `RoleInfo.aria_controls`，runtime 解析）。

**Tech Stack:** Rust（core + fence + packer + ffi），C#（Unity 投影），csbindgen FFI，pkg.bin v28→v29。

## Global Constraints

- **两台机约束**：本机（编码机）跑 headless 测试锁逻辑；Unity PlayMode 真机验收留家里机（本计划只到 headless + 重打 pkg，真机验收 defer）。
- **pkg 三向同步**（坑 66/158/177）：改 pkg 格式后必须 dll + GUI exe + 所有 fixture pkg.bin 三者同步重编/重打 + commit，否则 rc=-1 或渲染错。
- **加新 NodeKind/ControlState 变体必 grep 全 dispatch 点**（AGENTS.md 教训）：render arm、measure arm、cursor arm、clone、FFI setter or-pattern 等所有按 kind/state exhaustive match 的地方，逐一加 arm，否则控件半残。
- **Rust edition 2021**，依赖钉版本；CSS 选择器解析器手搓（零新依赖）。
- **push 前**：`cargo fmt --all -- --check` + `cargo clippy --all-targets -- -D warnings` + `cargo test`。
- **字段名一致性**：用 `selected_index`（镜像 Dropdown），**不**用 `active_index`（spec 草稿用过，实现统一改成 selected_index）。
- **NodeKind 判别值**：追加到 enum 末尾保持稳定——TabList=19、Tab=20（Template=18 之后）。C# NodeKind.cs 显式赋值同步（C# 跳过 Template=18 不影响，因显式赋值）。
- **变更同步**：任何 Rust 改动后重编 .dll + `cargo run -p xtask -- sync-bindings`；parse-time 逻辑改须重打 pkg。

**权威 spec**：`docs/superpowers/specs/2026-08-04-m3-tablist-design.md`。本计划实现层精确化了几处（见各 Task 注释），与 spec 决策一致。

---

## 文件结构（改动地图）

| 文件 | 责任 | 改动 |
|---|---|---|
| `crates/core/src/asset/mod.rs` | pkg 格式 + TemplateNode + ControlInit | v29 bump + `aria_controls` 字段 + 序列化 + ControlInit::TabList |
| `crates/core/src/asset/tests.rs` | pkg 测试 + `tn()` helper | v29 roundtrip/reject 测 + `tn()` 加字段 |
| `crates/fence/src/schema/tag.rs` | SemanticKind + ROLE_TO_SEMANTIC + resolve_semantic | 加 TabList/Tab |
| `crates/fence/src/control_structure_check.rs` | 控件结构契约 | tablist→tab 契约 |
| `crates/packer/pkg/src/bridge.rs` | IrTree→TemplateNode 桥 | 抽 aria-controls + map_semantic 两 arm + 初始 selected_index 派生 |
| `crates/core/src/scene/node.rs` | NodeKind + ControlState + RoleInfo | TabList/Tab 变体 + ControlState::TabList + RoleInfo.aria_controls |
| `crates/core/src/scene/dynamic.rs` | create_node_from_template + kind_tag | ControlInit→State TabList arm + kind_tag TabList/Tab + RoleInfo 填充 |
| `crates/core/src/scene/control.rs` | 控件逻辑（sync/on_pointer_down） | TabList arm（panel display + click 选中）+ role 常量 |
| `crates/core/src/style/dynamic.rs` | synth_aria_value + attr_matches_node | aria-selected 跨节点合成 |
| `crates/core/src/input.rs` | 输入路由 | TabList 方向键路由 |
| `crates/ffi/src/lib.rs` | C FFI | get/set_tablist_selected_index |
| `unity/package/Runtime/Projection/NodeKind.cs` | C# NodeKind enum | TabList=19, Tab=20 |
| `unity/package/Runtime/Projection/NodeFactory.cs` | kind→C# 类型 dispatch | TabList/Tab arm |
| `unity/package/Runtime/Public/LoomGUI.Nodes.cs` | C# 控件类 | TabList/Tab class |
| `tests/dotnet/LoomGUI.HeadlessTests/` | headless 测试 | TabListProjectionTests + fixture |
| `showcase/showcase/settings.html` | showcase 页 | 解注释 `[aria-selected]` 规则 |
| `docs/roadmap/{roadmap,milestones}.md` | 路线文档 | 对齐过期条目 |

---

## Task 1: pkg v29 — TemplateNode.aria_controls 字段

铺数据通路：让 `aria-controls` 字符串能从 HTML 经 pkg 活到 runtime。镜像现有 `role`/`data_slot` 的 StringTable interning 模式（**非** bincode整体——TemplateNode 是逐字段手动 LE 编码）。

**Files:**
- Modify: `crates/core/src/asset/mod.rs`（PKG_FORMAT_VERSION + MIN/MAX + TemplateNode struct + write_package + read_package）
- Modify: `crates/core/src/asset/tests.rs`（`tn()` helper + v29 测试）
- Test: `crates/core/src/asset/tests.rs`

**Interfaces:**
- Consumes: 无（基础数据层）
- Produces: `TemplateNode.aria_controls: Option<String>` 字段（后续 Task 4/6 用）；pkg 版本 = 29

- [ ] **Step 1: 改 PKG_FORMAT_VERSION + MIN/MAX（mod.rs:25-27）**

```rust
pub const PKG_FORMAT_VERSION: u32 = 29; // v29: TemplateNode.aria_controls (TabList panel linkage)
pub(crate) const MIN_VERSION: u32 = 29;
pub(crate) const MAX_VERSION: u32 = 29;
```

同步改 `mod.rs:1` 模块 doc 注释里的 `当前 version=28` → `29` + 加一行 v29 说明。

- [ ] **Step 2: TemplateNode 加字段（mod.rs:99-119 struct）**

在 `data_slot` 字段后加：
```rust
    /// WAI-ARIA `aria-controls`（TabList tab→panel 跨树关联的 panel id）。None = 非关联节点。
    /// 运行时由 instantiate 拷进 RoleInfo.aria_controls，sync_control_visuals 据此 find_node_by_id 解析 panel。
    pub aria_controls: Option<String>,
```

- [ ] **Step 3: write_package 序列化（mod.rs:177-189 node_records 元组 + intern 段 + 写段）**

`node_records` 元组类型加 `aria_controls_idx: u16`（变为 13-tuple）。在 intern 段（role/data_slot 旁）加：
```rust
    let aria_controls_idx = tn
        .aria_controls
        .as_ref()
        .map(|s| intern(&mut strings, s))
        .unwrap_or(NULL_IDX);
```
push record 处加字段 `aria_controls_idx`；写段（写 data_slot_idx 的下一行）加：
```rust
    out.extend_from_slice(&aria_controls_idx.to_le_bytes());
```

- [ ] **Step 4: read_package 反序列化（mod.rs:~440 读段 + ~462 构造）**

读段（读 data_slot_idx 的下一行）加：
```rust
    let aria_controls_idx = r.u16("aria_controls_idx")?;
    let aria_controls = (aria_controls_idx != NULL_IDX)
        .then(|| read_string(strings, aria_controls_idx));
```
TemplateNode 构造加 `aria_controls` 字段。

- [ ] **Step 5: 改 tn() helper（tests.rs:5-19）**

```rust
fn tn(kind: NodeKind) -> TemplateNode {
    TemplateNode {
        kind,
        style: ResolvedStyle::default(),
        parent_idx: None,
        classes: vec![],
        id_attr: None,
        draggable: false,
        tabindex: None,
        content: None,
        src: None,
        control_init: None,
        role: None,
        data_slot: None,
        aria_controls: None,
    }
}
```

- [ ] **Step 6: 写失败测试（tests.rs，镜像现有 v28 测改名 v29）**

```rust
#[test]
fn pkg_v29_roundtrip_with_aria_controls() {
    assert_eq!(
        PKG_FORMAT_VERSION, 29,
        "pkg format version must be 29 after aria_controls bump"
    );
    let mut node = tn(NodeKind::Container);
    node.aria_controls = Some("panel-audio".into());
    let nodes = [node];
    let rules = empty_rules();
    let input = PackageInput {
        components: vec![("c", &nodes, &rules)],
    };
    let pkg = read_package(&write_package(&input)).expect("roundtrip read ok");
    let back = &pkg.components["c"].nodes[0];
    assert_eq!(back.aria_controls.as_deref(), Some("panel-audio"));
}

#[test]
fn pkg_v29_rejects_v28() {
    let mut bad = vec![];
    bad.extend_from_slice(&PKG_MAGIC.to_le_bytes());
    bad.extend_from_slice(&28u32.to_le_bytes()); // v28 < MIN_VERSION=29
    let err = read_package(&bad);
    assert!(
        matches!(err, Err(PkgError::TooOld(28))),
        "v28 pkg must be rejected as TooOld after v29 bump, got {err:?}"
    );
}
```
删除旧的 `pkg_v28_roundtrip_with_role` / `pkg_v28_rejects_v27`（被 v29 取代；role/data_slot roundtrip 语义已由 v29 测覆盖，因同一 intern 路径）。若担心丢 role 测，v29 测里一并塞 role/data_slot/aria_controls 三字段断言。

- [ ] **Step 7: 跑测试确认绿**

Run: `cargo test -p loomgui_core asset::tests`
Expected: 新 v29 测 PASS；旧 v28 测已删不报。

- [ ] **Step 8: Commit**

```bash
git add crates/core/src/asset/
git commit -m "feat(pkg): v29 TemplateNode.aria_controls field (TabList panel linkage)"
```

---

## Task 2: fence — tablist/tab 语义分派 + 结构契约

让 fence 把 `role=tablist/tab` 解析成新 SemanticKind（→ bridge → NodeKind）。加打包期结构契约（tablist 须含 tab 子）。

**Files:**
- Modify: `crates/fence/src/schema/tag.rs`（SemanticKind + ROLE_TO_SEMANTIC + resolve_semantic）
- Modify: `crates/fence/src/control_structure_check.rs`（REQUIRED_CHILDREN + structure_hint）
- Modify: `crates/packer/pkg/src/bridge.rs`（map_semantic 两 arm + 抽 aria-controls + 初始 selected_index 派生）
- Test: `crates/fence/src/schema/tag.rs`（内联测）+ `crates/fence/src/control_structure_check.rs`

**Interfaces:**
- Consumes: Task 1 的 `TemplateNode.aria_controls` 字段
- Produces: `SemanticKind::TabList/Tab` → `NodeKind::TabList/Tab`（Task 3 加）；`aria_controls` 进 TemplateNode；`ControlInit::TabList{selected_index}` 初值

- [ ] **Step 1: SemanticKind 加变体（tag.rs SemanticKind enum）**

在 `ListItem,` 后、`Template,` 前加：
```rust
    ListView,
    ListItem,
    /// WAI-ARIA `role=tablist"` — tab 容器（→ NodeKind::TabList）。
    TabList,
    /// WAI-ARIA `role=tab"` — 单个 tab（→ NodeKind::Tab）。无状态，选中态从父 TabList.selected_index 派生。
    Tab,
    Template,
```

- [ ] **Step 2: ROLE_TO_SEMANTIC 加映射（tag.rs:101）**

```rust
    ("list", SemanticKind::ListView),
    ("listitem", SemanticKind::ListItem),
    ("tablist", SemanticKind::TabList),
    ("tab", SemanticKind::Tab),
];
```
注意：`tabpanel` **不加**（走 div→Container 透传，panel 靠 aria-controls 关联而非 role 分派）。

- [ ] **Step 3: 写失败测试 — resolve_semantic role 驱动（tag.rs 测试模块）**

```rust
#[test]
fn resolve_semantic_tablist_tab() {
    assert_eq!(
        resolve_semantic("div", Some("tablist"), false),
        Some(SemanticKind::TabList)
    );
    assert_eq!(
        resolve_semantic("button", Some("tab"), false),
        Some(SemanticKind::Tab)
    );
    // tabpanel 不分派：走 div → Container（panel 靠 aria-controls 关联）
    assert_eq!(
        resolve_semantic("div", Some("tabpanel"), false),
        Some(SemanticKind::Container)
    );
}
```

- [ ] **Step 4: 跑测试确认失败**

Run: `cargo test -p loomgui_fence resolve_semantic_tablist_tab`
Expected: FAIL（TabList/Tab 变体或测试不存在）—— Step 1-2 后实际会编译过 + PASS；若先写测试再改 enum，此处先红。视顺序调整。

- [ ] **Step 5: 结构契约 — REQUIRED_CHILDREN（control_structure_check.rs:22-34）**

```rust
const REQUIRED_CHILDREN: &[(&str, &[CheckSpec])] = &[
    ("combobox", &[CheckSpec::Role("listbox")]),
    ("listbox", &[CheckSpec::Role("option")]),
    ("slider", &[CheckSpec::Slot("thumb")]),
    ("progressbar", &[CheckSpec::Slot("fill")]),
    ("list", &[CheckSpec::Role("listitem")]),
    ("tablist", &[CheckSpec::Role("tab")]),
];
```

- [ ] **Step 6: structure_hint 加教学文案（control_structure_check.rs:131-145）**

```rust
        "list" => "a `<div role=\"list\">` needs at least one `role=\"listitem\"` child",
        "tablist" => "a `<div role=\"tablist\">` needs at least one `role=\"tab\"` child (panels link via aria-controls)",
```

- [ ] **Step 7: 写失败测试 — tablist 缺 tab 子报错（control_structure_check 测试）**

```rust
#[test]
fn tablist_without_tab_child_is_error() {
    let diags = check(r#"<div role="tablist"></div>"#);
    assert!(diags.iter().any(|d| d.code == DiagnosticCode::FenceMissingControlChild));
}

#[test]
fn tablist_with_tab_child_passes() {
    let diags = check(r#"<div role="tablist"><button role="tab" aria-controls="p1">A</button></div>"#);
    assert!(!diags.iter().any(|d| d.code == DiagnosticCode::FenceMissingControlChild));
}
```
（`check` / `DiagnosticCode` 名按现有测试模式对齐；参考现有 combobox 缺 listbox 的测。）

- [ ] **Step 8: bridge — map_semantic 加两 arm（bridge.rs:144-157）**

```rust
        Some(SemanticKind::ListItem) => Ok(NodeKind::ListItem),
        Some(SemanticKind::TabList) => Ok(NodeKind::TabList),
        Some(SemanticKind::Tab) => Ok(NodeKind::Tab),
        Some(SemanticKind::Template) => Ok(NodeKind::Template),
```
（依赖 Task 3 的 `NodeKind::TabList/Tab` 已存在——执行顺序上 Task 3 须先于本 Task 的 bridge 编译，或同 commit。建议 Task 2/3 合并执行分支。）

- [ ] **Step 9: bridge — 抽 aria-controls + 初始 selected_index（bridge.rs:49-66 Element 分支）**

在 `let data_slot = attr(el, "data-slot");` 后加：
```rust
                let aria_controls = attr(el, "aria-controls");
```
TemplateNode 构造加 `aria_controls` 字段（Element 分支 + Text 分支 `aria_controls: None`）。

初始 selected_index 派生：在 `extract_control_init(kind, el, ir_idx, &parsed.tree)` 调用前/后，若 kind 是 TabList，扫该 el 的 role=tab 子，找 `aria-selected="true"` 的序号：
```rust
                let control_init = if matches!(kind, NodeKind::TabList) {
                    let tabs = tab_children(el, &parsed.tree); // helper: 直接子里 role=tab 的 IrNode
                    let initial = tabs.iter().position(|t| attr(t, "aria-selected").as_deref() == Some("true"))
                        .unwrap_or(0);
                    Some(ControlInit::TabList { selected_index: initial as u32 })
                } else {
                    extract_control_init(kind, el, ir_idx, &parsed.tree)
                };
```
`tab_children` helper：在 bridge.rs 加，照 `attr` 模式——遍历 `parsed.tree.nodes[ir_idx].children`，取 IrNodeKind::Element 且 `attr(el,"role")=="tab"`。

- [ ] **Step 10: 写失败测试 — bridge 抽 aria-controls + 初始 selected_index（bridge.rs 测试模块，:483 附近）**

```rust
#[test]
fn tablist_aria_controls_and_initial_selected_extracted() {
    let nodes = bridged(
        r#"<style>[role="tab"][aria-selected="true"]{color:red}</style>
        <div role="tablist">
          <button role="tab" aria-controls="pa" aria-selected="false">A</button>
          <button role="tab" aria-controls="pb" aria-selected="true">B</button>
        </div>
        <div id="pa"></div><div id="pb"></div>"#,
    );
    let tablist = &nodes[0];
    assert_eq!(tablist.kind, NodeKind::TabList);
    assert_eq!(tablist.control_init, Some(ControlInit::TabList { selected_index: 1 }));
    // tabs 的 aria_controls 落到各自 TemplateNode
    let tab_b = &nodes[2]; // 按 DFS 序，第二个 tab
    assert_eq!(tab_b.aria_controls.as_deref(), Some("pb"));
}
```
（DFS 序索引以实际 bridged 返回为准，先 dump 确认；断言 kind==Tab + aria_controls + control_init。）

- [ ] **Step 11: doc-schema sync 门**

改 fence.md 对应段落（若 fence.md 列了 role 表，加 tablist/tab），跑 `cargo test -p loomgui_fence`（含 `doc_schema_sync` 交叉校验）。

- [ ] **Step 12: 跑测试 + fmt/clippy + Commit**

```bash
cargo test -p loomgui_fence && cargo test -p loomgui_pkg
cargo fmt --all -- --check && cargo clippy -p loomgui_fence -p loomgui_pkg -- -D warnings
git add crates/fence/ crates/packer/pkg/
git commit -m "feat(fence): tablist/tab semantic + structure contract + bridge aria-controls"
```

---

## Task 3: core — NodeKind::TabList/Tab + 全 dispatch 点

加 NodeKind 两变体，并 **grep 全 exhaustive match 点逐一加 arm**（AGENTS.md 教训：漏一个 = 控件半残）。

**Files:**
- Modify: `crates/core/src/scene/node.rs`（enum + from_u8 + is_container + exhaustive guard）
- Modify: `crates/core/src/style/dynamic.rs:270`（kind_tag 逆映射）
- Modify: 所有按 NodeKind exhaustive match 的点（grep 找）
- Test: `crates/core/src/scene/node.rs` 测试模块

**Interfaces:**
- Consumes: 无
- Produces: `NodeKind::TabList`（=19）、`NodeKind::Tab`（=20）；`from_u8(19/20)`、`is_container` 含两者、kind_tag 逆映射

- [ ] **Step 1: grep 全 NodeKind dispatch 点**

Run:
```bash
grep -rn "NodeKind::Template" crates/core/src/ | grep -v test
grep -rn "match.*\.kind\|match.*node\.kind\|match kind" crates/core/src/
```
每个出现 `NodeKind::Template`（当前末变体）的 exhaustive match 都要加 `TabList | Tab`。重点核对：`render/mod.rs`（render arm）、`layout/mod.rs`（measure arm）、`scene/control.rs`、`scene/dynamic.rs`、`style/dynamic.rs`、`hit.rs`（cursor/命中）、`stage.rs`。**列清单**，逐一确认。

- [ ] **Step 2: enum 加变体（node.rs:93-120）**

在 `Template,` 后加：
```rust
    Template,
    /// WAI-ARIA `role=tablist"` — tab 容器。ControlState::TabList{selected_index}。
    /// 子节点是 role=tab；panel 跨树靠 aria-controls 关联（RoleInfo.aria_controls）。
    TabList,
    /// WAI-ARIA `role=tab"` — 单个 tab。无 ControlState，aria-selected 从父 TabList.selected_index 派生。
    /// 容器型（持 label 子节点），镜像 Button。
    Tab,
}
```

- [ ] **Step 3: from_u8 加（node.rs:122-148）**

```rust
            18 => Some(NodeKind::Template),
            19 => Some(NodeKind::TabList),
            20 => Some(NodeKind::Tab),
            _ => None,
```

- [ ] **Step 4: exhaustive guard 加（node.rs:180-200 `_assert_from_u8_exhaustive`）**

match 里加 `| NodeKind::TabList | NodeKind::Tab`。

- [ ] **Step 5: is_container 加（node.rs:153-164）**

TabList（容器持 tab 子，镜像 ListView）+ Tab（持 label 子，镜像 Button）都入：
```rust
    pub fn is_container(self) -> bool {
        matches!(
            self,
            Self::Container
                | Self::TextElement
                | Self::Button
                | Self::ListView
                | Self::ListItem
                | Self::Slot
                | Self::CustomElement
                | Self::TabList
                | Self::Tab
        )
    }
```

- [ ] **Step 6: kind_tag 逆映射加（dynamic.rs:270-292）**

```rust
            NodeKind::Template => "template",
            NodeKind::TabList => "div",
            NodeKind::Tab => "button",
```
（Tab 默认 tag="button"——settings.html 用 `<button role=tab>`；`div role=tab` 的 tag 选择器 rematch 不命中 `button`，可接受，因 tab 几乎不靠 tag 选。若需 div 也命中，逆映射给 "div"——按 showcase 实际用法定，默认 button。）

- [ ] **Step 7: 逐一加 Step 1 清单里的 dispatch arm**

对 render arm / measure arm / cursor / hit / clone 等每个 exhaustive match，加 `NodeKind::TabList | NodeKind::Tab => <对应默认行为>`。多数情况：TabList/Tab 走 Container/Button 同路（render 当普通容器、measure 当容器、cursor pointer）。**照 Dropdown/OptionItem 在同处的处理**镜像。

- [ ] **Step 8: 写测试 — from_u8 + is_container（node.rs 测试模块）**

```rust
#[test]
fn tablist_tab_kind_roundtrip_and_container() {
    assert_eq!(NodeKind::from_u8(19), Some(NodeKind::TabList));
    assert_eq!(NodeKind::from_u8(20), Some(NodeKind::Tab));
    assert!(NodeKind::TabList.is_container());
    assert!(NodeKind::Tab.is_container());
}
```

- [ ] **Step 9: 编译 + 跑测试 + fmt/clippy**

```bash
cargo build -p loomgui_core
cargo test -p loomgui_core
cargo fmt --all -- --check && cargo clippy -p loomgui_core --all-targets -- -D warnings
```
Expected: 编译过（所有 exhaustive match 已加 arm 是编译前提）；测试绿。

- [ ] **Step 10: Commit**

```bash
git add crates/core/src/
git commit -m "feat(core): NodeKind::TabList/Tab + all dispatch arms"
```

---

## Task 4: core — ControlState::TabList + ControlInit + RoleInfo.aria_controls

加控件状态 + pkg 初值 + runtime aria-controls 存储 + instantiate 填充。

**Files:**
- Modify: `crates/core/src/scene/node.rs`（ControlState enum + RoleInfo struct）
- Modify: `crates/core/src/asset/mod.rs`（ControlInit enum）
- Modify: `crates/core/src/scene/dynamic.rs`（create_node_from_template ControlInit→State + RoleInfo 填充）
- Modify: 所有按 ControlState exhaustive match 的点（grep）
- Test: `crates/core/src/scene/dynamic.rs` 测试模块

**Interfaces:**
- Consumes: Task 1 `ControlInit`、Task 3 NodeKind
- Produces: `ControlState::TabList { selected_index: usize }`、`RoleInfo.aria_controls`、instantiate 时 RoleInfo 填 aria_controls

- [ ] **Step 1: grep 全 ControlState dispatch 点**

```bash
grep -rn "ControlState::NumberField\|ControlState::Dropdown" crates/core/src/ | grep -v test
```
每个 exhaustive match（sync_control_visuals、on_pointer_down、synth_aria_value、render arm、measure_text_controls、FFI getter/setter or-pattern 等）要加 TabList arm。列清单。

- [ ] **Step 2: ControlState 加变体（node.rs:415-449）**

在 `NumberField { .. }` 后加：
```rust
    /// WAI-ARIA `role=tablist"`。selected_index=当前激活 tab 序号（无 aria-selected 存储于 Tab，
    /// 由 synth_aria_value 从父 selected_index 派生）。无 value_lock（aria-selected 是只读合成，无回写环）。
    TabList {
        selected_index: usize,
    },
```

- [ ] **Step 3: ControlInit 加变体（asset/mod.rs:56-104）**

在 `NumberField { .. }` 后加：
```rust
    /// `role=tablist"` 初始激活 tab 序号（打包期扫 role=tab 子的 aria-selected="true" 算出，无则 0）。
    TabList {
        selected_index: u32,
    },
```

- [ ] **Step 4: ControlInit→ControlState 映射（dynamic.rs:225-289 create_node_from_template）**

在 `ControlInit::NumberField { .. } => {...}` arm 后加：
```rust
            ControlInit::TabList { selected_index } => ControlState::TabList {
                selected_index: selected_index as usize,
            },
```

- [ ] **Step 5: RoleInfo 加 aria_controls（node.rs:503-568）**

```rust
pub struct RoleInfo {
    pub role: Option<String>,
    pub slots: std::collections::HashMap<String, String>,
    /// WAI-ARIA `aria-controls`（TabList tab→panel 跨树关联的 panel id 字符串）。
    /// None = 非关联节点。instantiate 从 TemplateNode.aria_controls 拷入。
    pub aria_controls: Option<String>,
}
```
所有构造 `RoleInfo { role, slots }` 处加 `aria_controls`（默认 None）。grep `RoleInfo {` 找全。

- [ ] **Step 6: instantiate 填 RoleInfo.aria_controls（dynamic.rs instantiate/build 路径）**

定位 instantiate 从 TemplateNode 填 RoleTable 的地方（搜 `RoleTable::insert` / `roles.insert` / `RoleInfo {`，在 stage.rs 或 dynamic.rs）。在填 role/data_slot 处，同步：
```rust
    let aria_controls = tpl.aria_controls.clone(); // TemplateNode.aria_controls
    // 进 RoleInfo
    scene.roles.insert_or_update(id, RoleInfo { role, slots, aria_controls });
```
（具体 API 名按现有 RoleTable.insert 签名对齐；aria_controls 与 role/data_slot 同源同填。）

- [ ] **Step 7: 逐一加 Step 1 清单里的 ControlState dispatch arm**

- `sync_control_visuals`（control.rs:540）→ Task 6 详写（本 Task 先加 no-op `ControlState::TabList { .. } => {}` 占位，Task 6 填）。
- `on_pointer_down`（control.rs:704）→ Task 7 详写（本 Task 先占位 no-op）。
- `synth_aria_value`（dynamic.rs:378）→ Task 5 详写。
- 其他（render/measure/FFI getter setter）：TabList 走「非视觉控件，no-op 或默认」分支（同 Dropdown 在该处的处理）。

- [ ] **Step 8: 写测试 — ControlInit→State 映射 + RoleInfo.aria_controls（dynamic.rs 测试）**

```rust
#[test]
fn tablist_control_init_maps_to_state() {
    let init = ControlInit::TabList { selected_index: 2 };
    // 经 create_node_from_template（或直接测映射函数）
    // ... 构造一个 TabList NodeKind + control_init=init 的 template → instantiate ...
    assert!(matches!(scene.controls.get(id), Some(ControlState::TabList { selected_index: 2 })));
}
```
（具体 harness 按 dynamic.rs 现有 instantiate 测试模式。）

- [ ] **Step 9: 编译 + 测试 + fmt/clippy + Commit**

```bash
cargo build -p loomgui_core && cargo test -p loomgui_core
cargo fmt --all -- --check && cargo clippy -p loomgui_core --all-targets -- -D warnings
git commit -am "feat(core): ControlState::TabList + ControlInit + RoleInfo.aria_controls"
```

---

## Task 5: core — aria-selected 跨节点合成

让 `[aria-selected="true"]` 选择器对 Tab 命中：Tab 的 aria-selected 从父 TabList.selected_index 派生。

**Files:**
- Modify: `crates/core/src/style/dynamic.rs:378`（synth_aria_value）
- Test: `crates/core/src/style/dynamic.rs` 测试模块

**Interfaces:**
- Consumes: Task 3 NodeKind::Tab + Task 4 ControlState::TabList
- Produces: `synth_aria_value(id, "selected")` 对 Tab 返回 "true"/"false"

- [ ] **Step 1: 写失败测试 — aria-selected 跨节点合成**

构造 scene：TabList(selected_index=1) + 3 个 Tab 子。断言：
```rust
#[test]
fn tab_aria_selected_synth_from_parent_tablist() {
    let mut scene = Scene::test();
    let tl = scene.create(NodeKind::TabList);
    let t0 = scene.create(NodeKind::Tab);
    let t1 = scene.create(NodeKind::Tab);
    scene.attach(tl, [t0, t1]);
    scene.roles.set(t0, RoleInfo { role: Some("tab".into()), ..default() });
    scene.roles.set(t1, RoleInfo { role: Some("tab".into()), ..default() });
    scene.controls.ensure(tl, ControlState::TabList { selected_index: 1 });

    assert_eq!(synth_aria_value(&scene, t0, "selected"), Some("false".into()));
    assert_eq!(synth_aria_value(&scene, t1, "selected"), Some("true".into()));
    // 非 Tab 节点：aria-selected 无语义
    assert_eq!(synth_aria_value(&scene, tl, "selected"), None);
}
```
（Scene::test/attach/roles.set 等 helper 名按现有 dynamic.rs 测试模式对齐。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p loomgui_core tab_aria_selected_synth_from_parent_tablist`
Expected: FAIL（synth 当前对 "selected" 返回 None）。

- [ ] **Step 3: 实现 — synth_aria_value 加 selected 分支（dynamic.rs:378-410）**

在函数开头（aria-multiline 静态判分支旁）加 Tab 的跨节点合成：
```rust
fn synth_aria_value(scene: &Scene, id: NodeId, aria: &str) -> Option<String> {
    // aria-multiline 静态：按 NodeKind 判（TextArea vs TextField）。
    if aria == "multiline" {
        return match scene.get(id).map(|n| n.kind) {
            Some(NodeKind::TextArea) => Some("true".to_string()),
            _ => None,
        };
    }
    // aria-selected：Tab 的选中态从父 TabList.selected_index 跨节点派生（Tab 自身无 ControlState）。
    if aria == "selected" {
        let node = scene.get(id)?;
        if node.kind != NodeKind::Tab {
            return None; // 非 Tab：aria-selected 无语义
        }
        // 向上找最近 TabList 祖先
        let tablist = scene.ancestors_of(id).find(|&a| scene.get(a).is_some_and(|n| n.kind == NodeKind::TabList))?;
        let selected_index = match scene.controls.get(tablist) {
            Some(ControlState::TabList { selected_index }) => *selected_index,
            _ => return None,
        };
        // 本 Tab 在父 role=tab 子里的序号
        let my_index = scene.get(tablist)?.children.iter()
            .filter(|&&c| scene.roles.role_of(c) == Some("tab"))
            .position(|&c| c == id)?;
        return Some((my_index == selected_index).to_string());
    }
    let cs = scene.controls.get(id)?;
    Some(match (aria, cs) {
        // ...existing arms unchanged...
    })
}
```
`scene.ancestors_of(id)` —— 若无此 API，用现有 parent 链向上走（`scene.get(id).parent` 循环）。按现有代码对齐。

- [ ] **Step 4: 跑测试确认绿**

Run: `cargo test -p loomgui_core tab_aria_selected_synth_from_parent_tablist`
Expected: PASS。

- [ ] **Step 5: 集成测试 — attr_matches_node 命中 `[aria-selected="true"]`**

```rust
#[test]
fn attr_selector_aria_selected_hits_active_tab() {
    // 同上 scene 构造，selected_index=1
    let sel_true = AttrSelector { name: "aria-selected".into(), op: AttrOp::Eq, value: Some("true".into()) };
    assert!(!attr_matches_node(&scene, t0, &sel_true));
    assert!(attr_matches_node(&scene, t1, &sel_true));
}
```

- [ ] **Step 6: fmt/clippy + Commit**

```bash
cargo fmt --all -- --check && cargo clippy -p loomgui_core -- -D warnings
git commit -am "feat(core): synth aria-selected for Tab from parent TabList"
```

---

## Task 6: core — sync_control_visuals TabList arm（panel 显隐 P1）

非激活 panel 的 display 置 none，激活的置回自然值。复用 display:none 剪枝（镜像 Dropdown 切 listbox display）。

**Files:**
- Modify: `crates/core/src/scene/control.rs:540`（sync_control_visuals）
- Modify: `crates/core/src/scene/control.rs:34`（role 常量）
- Test: `crates/core/src/scene/control.rs` 测试模块

**Interfaces:**
- Consumes: Task 4 RoleInfo.aria_controls + ControlState::TabList + Task 3 NodeKind
- Produces: 每帧非激活 panel display=none（激活 panel 自然值）

- [ ] **Step 1: role 常量（control.rs:34）**

```rust
pub const ROLE_TABLIST: &str = "tablist";
pub const ROLE_TAB: &str = "tab";
```

- [ ] **Step 2: 写失败测试 — panel display 随 selected_index 切**

构造 scene：TabList + 2 tab（aria_controls="pa"/"pb"）+ 2 panel（id pa/pb，初始 display 可见）。RoleInfo 填 aria_controls。selected_index=0。调 sync_control_visuals(tablist)。断言 panel pa 的 resolved display != None、panel pb 的 resolved display == None。改 selected_index=1 再 sync，断言反转。

```rust
#[test]
fn tablist_panel_display_follows_selected_index() {
    let mut scene = Scene::test();
    let tl = scene.create(NodeKind::TabList);
    let t0 = scene.create_with_role(NodeKind::Tab, "tab", RoleInfo { aria_controls: Some("pa".into()), ..default() });
    let t1 = scene.create_with_role(NodeKind::Tab, "tab", RoleInfo { aria_controls: Some("pb".into()), ..default() });
    let pa = scene.create_with_id(NodeKind::Container, "pa");
    let pb = scene.create_with_id(NodeKind::Container, "pb");
    scene.attach(tl, [t0, t1]);
    scene.controls.ensure(tl, ControlState::TabList { selected_index: 0 });

    sync_control_visuals(&mut scene, tl);
    assert!(resolved_display(&scene, pa) != DisplayMode::None, "active panel pa visible");
    assert_eq!(resolved_display(&scene, pb), DisplayMode::None, "inactive panel pb hidden");

    set_selected_index(&mut scene, tl, 1);
    sync_control_visuals(&mut scene, tl);
    assert_eq!(resolved_display(&scene, pa), DisplayMode::None);
    assert!(resolved_display(&scene, pb) != DisplayMode::None);
}
```
（`resolved_display` helper 读 ResolvedStyle.display_mode；`create_with_role`/`create_with_id` 按 scene 测试 harness 现有模式。）

- [ ] **Step 3: 跑测试确认失败**

Expected: FAIL（TabList arm 当前 no-op，panel display 不变）。

- [ ] **Step 4: 实现 — sync_control_visuals TabList arm（control.rs:540-679）**

在 Dropdown arm 后加：
```rust
        ControlState::TabList { selected_index } => {
            // 非激活 panel display=none，激活 panel 置回自然值（清 control 强制的 none）。
            // panel 跨树：遍历 role=tab 子，读其 RoleInfo.aria_controls（panel id 串）→ find_node_by_id 解析。
            let tab_ids: Vec<NodeId> = scene.get(id)
                .map(|n| n.children.clone()).unwrap_or_default()
                .into_iter()
                .filter(|&c| scene.roles.role_of(c) == Some(ROLE_TAB))
                .collect();
            for (i, &tab) in tab_ids.iter().enumerate() {
                let Some(panel_id_str) = scene.roles.get(tab).and_then(|r| r.aria_controls.clone()) else {
                    continue; // tab 没写 aria-controls：无 panel 可切（R1 容错）
                };
                let Some(&panel) = scene.id_index.get(&panel_id_str) else {
                    continue; // panel id 找不到（R1 容错；fence 期已校验 idref 存在，运行时动态缺则跳）
                };
                let decl = if i == selected_index { "display:block" } else { "display:none" };
                let _ = set_inline_override(scene, panel, decl);
            }
        }
```
`scene.id_index`（id_attr 字符串→NodeId 反查）—— 若无此字段，用现有 `find_node_by_id`（全局首匹配）。`set_inline_override` 已存在（dynamic.rs:646 INLINE_DISPLAY bit 路径，Dropdown 切 listbox 用的就是它）。激活 panel 用 "display:block" 覆盖作者可能的 "display:none"（settings.html panel 初始 style="display:none"，激活时翻 block）。

- [ ] **Step 5: 跑测试确认绿**

Run: `cargo test -p loomgui_core tablist_panel_display_follows_selected_index`
Expected: PASS。

- [ ] **Step 6: fmt/clippy + Commit**

```bash
cargo fmt --all -- --check && cargo clippy -p loomgui_core -- -D warnings
git commit -am "feat(core): sync_control_visuals TabList — panel display follows selected_index"
```

---

## Task 7: core — click 激活 + 方向键 + SelectionChanged

click tab → 改 selected_index + 发 SelectionChanged；方向键移动 selected_index（K1）。

**Files:**
- Modify: `crates/core/src/scene/control.rs:704`（on_pointer_down TabList/Tab arm）
- Modify: `crates/core/src/input.rs:563`（键盘路由）
- Test: 两处测试模块

**Interfaces:**
- Consumes: Task 4 ControlState::TabList + Task 6 panel 机制
- Produces: click/键盘改 selected_index + `EVT_SELECTION_CHANGED` 发出

- [ ] **Step 1: click — 写失败测试**

```rust
#[test]
fn click_tab_sets_selected_index_and_emits() {
    let mut scene = /* TabList + 2 tab 构造，selected_index=0 */;
    let events = on_pointer_down(&mut scene, t1, [0.0, 0.0]);
    assert!(matches!(scene.controls.get(tl), Some(ControlState::TabList { selected_index: 1 })));
    assert!(events.iter().any(|e| e.event_type == EVT_SELECTION_CHANGED && e.touch_id == 1));
}
```

- [ ] **Step 2: click — 实现 on_pointer_down（control.rs:704-789）**

on_pointer_down 命中 Tab 节点时，向上找 TabList，算本 tab 序号，改 selected_index，发事件。在现有 match 加（命中控件根 id 是 TabList 的情况；Tab 是子，find_control_at 向上找到 TabList）：

注：find_control_at 向上找最近有 ControlState 的节点。Tab 无 ControlState，故点 Tab → find_control_at 找到父 TabList。故 on_pointer_down 的 id 参数是 TabList。需判定命中点是否落在某 role=tab 子内：

```rust
        ControlState::TabList { mut selected_index } => {
            // 命中点落在哪个 role=tab 子内 → 设为 selected_index。
            let tab_ids: Vec<NodeId> = scene.get(id).map(|n| n.children.clone()).unwrap_or_default()
                .into_iter().filter(|&c| scene.roles.role_of(c) == Some(ROLE_TAB)).collect();
            for (i, &tab) in tab_ids.iter().enumerate() {
                // 命中判定：复用现有 rect-contains 模式（照 control.rs `dropdown_option_at_pos`
                // 的实现——读 tab 的 layout_rect 含 pos）。若该 helper 不可复用，inline 写 rect 包含判定。
                if node_rect_contains(scene, tab, pos) {
                    if selected_index != i {
                        selected_index = i;
                        out.push(EventRecord {
                            node_id: id.0,
                            event_type: EVT_SELECTION_CHANGED,
                            click_count: 0, pad: [0,0], touch_id: i as i32, x: 0.0, y: 0.0,
                        });
                    }
                    break;
                }
            }
            if let Some(cs) = scene.controls.get_mut(id) {
                if let ControlState::TabList { selected_index: s, .. } = cs { *s = selected_index; }
            }
        }
```
`hit_test_node` —— 用现有命中判定（scene 里节点 layout_rect 含 pos）。命中靠上帧 layout_rect（process 阶段）。按现有 Dropdown `dropdown_option_at_pos` 的命中模式镜像。

- [ ] **Step 3: click — 跑测试确认绿**

Run: `cargo test -p loomgui_core click_tab_sets_selected_index_and_emits`

- [ ] **Step 4: 键盘 — 写失败测试**

焦点在 TabList 子树时，方向键移动 selected_index（row=Left/Right，column=Up/Down）。

```rust
#[test]
fn arrow_key_moves_tablist_selected_index() {
    let mut scene = /* TabList(selected_index=0) + 3 tab, flex-direction=row */;
    scene.focused = Some(tl);
    process_key(&mut scene, KeyCode::Right);
    assert!(matches!(scene.controls.get(tl), Some(ControlState::TabList { selected_index: 1 })));
    process_key(&mut scene, KeyCode::Left);
    assert!(matches!(scene.controls.get(tl), Some(ControlState::TabList { selected_index: 0 })));
    process_key(&mut scene, KeyCode::Right); process_key(&mut scene, KeyCode::Right); // wrap or clamp
    assert!(matches!(scene.controls.get(tl), Some(ControlState::TabList { selected_index } if selected_index <= 2)));
}
```

- [ ] **Step 5: 键盘 — 实现 input.rs TabList 路由（input.rs:563 附近，Dropdown 键盘路由同处）**

```rust
                // TabList 键盘路由：焦点在 TabList 时，方向键移动 selected_index（自动激活）。
                let is_tablist_focused = /* 焦点节点是 TabList 或其 Tab 子（向上找 TabList） */;
                if is_tablist_focused {
                    let tl = /* 焦点向上的 TabList NodeId */;
                    let tab_count = /* role=tab 子数 */;
                    if tab_count == 0 { /* skip */ }
                    else {
                        let dir = tablist_arrow_dir(scene, tl, code); // row→Left=-1/Right=+1; column→Up/Down
                        if let Some(delta) = dir {
                            let new = (current + delta as isize).max(0).min(tab_count as isize - 1) as usize; // clamp（不做 wrap）
                            set_tablist_selected_index(scene, tl, new); // 复用：改 state + 发 SelectionChanged
                        }
                    }
                }
```
`tablist_arrow_dir`：读 TabList 的 `flex-direction`（resolved taffy_style.flex_direction）：row/row-reverse → Left/Right；column/column-reverse → Up/Down。row-reverse/column-reverse 翻转方向。`set_tablist_selected_index` 抽公共 helper（click 和键盘共用：改 state + 发事件）。

- [ ] **Step 6: 键盘 — 跑测试确认绿**

Run: `cargo test -p loomgui_core arrow_key_moves_tablist_selected_index`

- [ ] **Step 7: fmt/clippy + Commit**

```bash
cargo fmt --all -- --check && cargo clippy -p loomgui_core -- -D warnings
git commit -am "feat(core): TabList click + arrow-key activation + SelectionChanged"
```

---

## Task 8: FFI — get/set_tablist_selected_index

C ABI 读写 TabList.selected_index（镜像 Dropdown 的 get/set_dropdown_selected_index）。

**Files:**
- Modify: `crates/ffi/src/lib.rs`（两个 extern fn，紧挨 dropdown 那对）
- Test: `crates/ffi/src/tests.rs`

**Interfaces:**
- Consumes: Task 4 ControlState::TabList
- Produces: `loomgui_stage_get_tablist_selected_index` / `loomgui_stage_set_tablist_selected_index`（csbindgen 自动生成 C# 签名）

- [ ] **Step 1: 写失败测试（ffi/tests.rs，镜像 dropdown 测）**

```rust
#[test]
fn tablist_selected_index_ffi_roundtrip() {
    let (mut sh, tl) = /* harness：建 TabList 节点 + ControlState::TabList{selected_index:0} */;
    let mut out = 0u32;
    let rc = unsafe { loomgui_stage_get_tablist_selected_index(sh.as_ptr(), tl, &mut out) };
    assert_eq!(rc, 0); assert_eq!(out, 0);
    let rc = unsafe { loomgui_stage_set_tablist_selected_index(sh.as_mut_ptr(), tl, 2) };
    assert_eq!(rc, 0);
    let rc = unsafe { loomgui_stage_get_tablist_selected_index(sh.as_ptr(), tl, &mut out) };
    assert_eq!(out, 2);
}
```

- [ ] **Step 2: 实现（lib.rs，照 lib.rs:2411-2467 dropdown 那对镜像）**

```rust
#[no_mangle]
pub extern "C" fn loomgui_stage_get_tablist_selected_index(
    h: *const StageHandle, node_id: u32, out: *mut u32,
) -> i32 {
    if h.is_null() || out.is_null() { return -1; }
    let sh = unsafe { &*h };
    let Some(scene) = sh.stage.scene.as_ref() else { return -1; };
    match scene.controls.get(NodeId(node_id)) {
        Some(ControlState::TabList { selected_index }) => {
            unsafe { *out = *selected_index as u32; }
            0
        }
        _ => -1,
    }
}

#[no_mangle]
pub extern "C" fn loomgui_stage_set_tablist_selected_index(
    h: *mut StageHandle, node_id: u32, index: u32,
) -> i32 {
    if h.is_null() { return -1; }
    let sh = unsafe { &mut *h };
    let Some(scene) = sh.stage.scene.as_mut() else { return -1; };
    if let Some(ControlState::TabList { selected_index }) = scene.controls.get_mut(NodeId(node_id)) {
        *selected_index = index as usize;
        0
    } else { -1 }
}
```
注：TabList 无 value_lock（aria-selected 只读合成，无回写环），setter 不设 value_lock。事件发射在 tick（on_pointer_down/键盘已发），setter 只改态。

- [ ] **Step 3: 跑测试确认绿 + 重编 dll + sync-bindings**

```bash
cargo test -p loomgui_ffi_c
cargo build -p loomgui_ffi_c --release
# Unity 须关着：
cp target/release/loomgui_ffi_c.dll unity/package/Plugins/LoomGUI/loomgui_ffi_c.dll
cargo run -p xtask -- sync-bindings
```

- [ ] **Step 4: Commit**

```bash
git add crates/ffi/ unity/package/Plugins/LoomGUI/loomgui_ffi_c.dll unity/package/Plugins/LoomGUI/Bindings/LoomGUIBindings.cs
git commit -m "feat(ffi): get/set_tablist_selected_index"
```

---

## Task 9: C# 投影 + Headless 测试

C# NodeKind enum + TabList/Tab class + NodeFactory dispatch + Headless fixture/tests。

**Files:**
- Modify: `unity/package/Runtime/Projection/NodeKind.cs`（加 TabList=19, Tab=20）
- Modify: `unity/package/Runtime/Projection/NodeFactory.cs`（dispatch 两 arm）
- Modify: `unity/package/Runtime/Public/LoomGUI.Nodes.cs`（TabList/Tab class）
- Create: `tests/dotnet/LoomGUI.HeadlessTests/TabListProjectionTests.cs`
- Create: `tests/dotnet/LoomGUI.HeadlessTests/fixtures/tablist.workspace` + 打 `tablist.pkg.bin`

**Interfaces:**
- Consumes: Task 8 FFI（C# 签名已由 sync-bindings 生成）
- Produces: `TabList`（: Container，SelectedIndex + SelectionChanged）、`Tab`（: Container）

- [ ] **Step 1: NodeKind.cs 加变体**

```csharp
        CustomElement = 17,
        // Template=18 在 Rust 存在但 C# 不暴露（display:none 蓝图，不实例化为 live node）。
        TabList = 19,
        Tab = 20,
```

- [ ] **Step 2: NodeFactory.cs dispatch 加 arm**

```csharp
                NodeKind.OptionItem     => new OptionItem(ctx, id),
                NodeKind.Slot           => new Slot(ctx, id),
                NodeKind.CustomElement  => new CustomElement(ctx, id),
                NodeKind.TabList        => new TabList(ctx, id),
                NodeKind.Tab            => new Tab(ctx, id),
```

- [ ] **Step 3: TabList class（LoomGUI.Nodes.cs，照 Dropdown:1970-2044，但 : Container 因持 tab 子）**

```csharp
    public unsafe class TabList : Container
    {
        internal TabList(UIContext ctx, uint id) : base(ctx, id) { }

        public int SelectedIndex
        {
            get { ThrowIfDisposed(); return (int)GetTabListSelectedIndex(); }
            set { ThrowIfDisposed(); SetTabListSelectedIndex((uint)value); }
        }
        public bool Disabled { set { ThrowIfDisposed(); SetNodeDisabled(value); } get { ThrowIfDisposed(); return GetNodeDisabled(); } }

        [NonSerialized] Dictionary<Action<SelectionChangedEvent>, EventRegistration> _selectionChangedBacking;
        public event Action<SelectionChangedEvent> SelectionChanged
        {
            add
            {
                if (value == null) return;
                if (_selectionChangedBacking == null)
                    _selectionChangedBacking = new Dictionary<Action<SelectionChangedEvent>, EventRegistration>();
                if (_selectionChangedBacking.ContainsKey(value)) return;
                var reg = On<ControlSelectionChangedEvent>(e => value(new SelectionChangedEvent { _oldIndex = -1, _newIndex = e.NewIndex }));
                _selectionChangedBacking[value] = reg;
            }
            remove
            {
                if (_selectionChangedBacking != null && _selectionChangedBacking.TryGetValue(value, out var reg))
                {
                    _selectionChangedBacking.Remove(value);
                    reg.Dispose();
                }
            }
        }

        StageHandle* Handle() => (StageHandle*)_ctx._stage.ToPointer();
        uint GetTabListSelectedIndex()
        {
            StageHandle* h = Handle();
            uint v = 0; int rc = Native.loomgui_stage_get_tablist_selected_index(h, _id, &v);
            if (rc != 0) throw new InvalidOperationException($"get_tablist_selected_index failed (node {_id})");
            return v;
        }
        void SetTabListSelectedIndex(uint v)
        {
            StageHandle* h = Handle();
            int rc = Native.loomgui_stage_set_tablist_selected_index(h, _id, v);
            if (rc != 0) throw new InvalidOperationException($"set_tablist_selected_index failed (node {_id})");
        }
        void SetNodeDisabled(bool v) { Native.loomgui_stage_set_node_disabled(Handle(), _id, v); }
        bool GetNodeDisabled() { byte b = 0; Native.loomgui_stage_get_node_disabled(Handle(), _id, &b); return b != 0; }
    }
```
SelectionChanged 复用 `ControlSelectionChangedEvent` + `SelectionChangedEvent`（Dropdown 已在，零新增 event struct）。

- [ ] **Step 4: Tab class（照 OptionItem:2053-2081）**

```csharp
    public unsafe class Tab : Container
    {
        internal Tab(UIContext ctx, uint id) : base(ctx, id) { }
        // selected 从父 TabList.SelectedIndex 派生（core 合成 aria-selected），无 per-tab getter FFI。
        public bool Selected { get { throw NE(); } }
        public bool Disabled { set { ThrowIfDisposed(); SetNodeDisabled(value); } get { ThrowIfDisposed(); return GetNodeDisabled(); } }
        StageHandle* Handle() => (StageHandle*)_ctx._stage.ToPointer();
        void SetNodeDisabled(bool v) { Native.loomgui_stage_set_node_disabled(Handle(), _id, v); }
        bool GetNodeDisabled() { byte b = 0; Native.loomgui_stage_get_node_disabled(Handle(), _id, &b); return b != 0; }
        static NotImplementedException NE() => new NotImplementedException();
    }
```

- [ ] **Step 5: 建 fixture（tablist.workspace + tablist.html）**

`tests/dotnet/LoomGUI.HeadlessTests/fixtures/tablist.html`：
```html
<style>[role="tab"][aria-selected="true"]{color:#e94560;font-weight:700}</style>
<div role="tablist">
  <button role="tab" id="t-a" aria-controls="p-a" aria-selected="true">A</button>
  <button role="tab" id="t-b" aria-controls="p-b" aria-selected="false">B</button>
</div>
<div id="p-a" class="panel">panel A</div>
<div id="p-b" class="panel" style="display:none">panel B</div>
```
`tablist.workspace`（照现有 fixture .workspace 格式，指 tablist.html + 组件名 tablist）。
重打：`cargo run -p loomgui_pkg -- build tests/dotnet/LoomGUI.HeadlessTests/fixtures/tablist.workspace`。

- [ ] **Step 6: 写 Headless 测试（照 DropdownProjectionTests 模式）**

```csharp
public class TabListProjectionTests
{
    [Fact]
    public void tablist_selected_index_roundtrips()
    {
        var (stage, ctx, root) = LoadTabListFixture();
        try {
            var tl = root.Get<TabList>("tablist-id"); // fixture 给 tablist 一个 id（补 id="tl" 到 html）
            Assert.Equal(0, tl.SelectedIndex); // 打包期 aria-selected=true 在 t-a（index 0）
            tl.SelectedIndex = 1;
            Assert.Equal(1, tl.SelectedIndex);
        } finally { StageHarness.Destroy(stage); }
    }

    [Fact]
    public void tablist_factory_dispatches_typed()
    {
        var (stage, ctx, root) = LoadTabListFixture();
        try {
            Assert.IsType<TabList>(root.Query<TabList>().First()); // 或 Get<TabList>
            Assert.IsType<Tab>(root.Query<Tab>().First());
        } finally { StageHarness.Destroy(stage); }
    }

    [Fact]
    public void tablist_selection_changed_via_demux()
    {
        var (stage, ctx, root) = LoadTabListFixture();
        try {
            var tl = root.Get<TabList>("tl");
            SelectionChangedEvent received = default;
            tl.SelectionChanged += e => received = e;
            using (var buf = new NativeEventBuffer()) {
                buf.Add(tl._id, (byte)EventType.SelectionChanged, touchId: 1);
                ctx._eventDemuxer.Pump(buf.Ptr, buf.Count);
            }
            Assert.Equal(1, received.NewIndex);
        } finally { StageHarness.Destroy(stage); }
    }
}
```
`LoadTabListFixture` 照 `LoadDropdownFixture`（6c）：CreateRoot + LoadPackage("tablist") + Instantiate + AppendChild + Tick。fixture html 里给 tablist `id="tl"` 便于 Get。

- [ ] **Step 7: 跑 dotnet 测试**

```bash
dotnet test tests/dotnet/LoomGUI.HeadlessTests/
dotnet build tests/dotnet/LoomGUI.PublicApi/LoomGUI.PublicApi.csproj
```
Expected: 新 TabList 测 PASS；PublicApi 编译门绿。

- [ ] **Step 8: Commit**

```bash
git add unity/package/Runtime/ tests/dotnet/LoomGUI.HeadlessTests/
git commit -m "feat(c#): TabList/Tab projection + headless tests"
```

---

## Task 10: 文档对齐 + settings.html + showcase 重打 + 收口

修 roadmap/milestones 过期条目、解注释 settings.html、重打 showcase、全门绿。

**Files:**
- Modify: `docs/roadmap/roadmap.md`（§4 tech-debt aria-selected 条改写）
- Modify: `docs/roadmap/milestones.md`（M3 exit criteria）
- Modify: `showcase/showcase/settings.html:17`（解注释 CSS）
- Modify: `docs/superpowers/specs/2026-08-04-m3-tablist-design.md`（补 SemanticKind 链 + selected_index 命名 + RoleInfo.aria_controls 精确化）
- 重打: `unity/showcase-unity/Assets/Bundles/ui/showcase.pkg.bin`

**Interfaces:**
- Consumes: Task 1-9 全绿
- Produces: 文档与实现一致；showcase settings 页 pkg v29

- [ ] **Step 1: spec 精确化回填**

在 spec §5.1 补「SemanticKind::TabList/Tab（fence enum）→ map_semantic → NodeKind」；§5.2/§4 表把 `active_index` 改 `selected_index`；§5.4 把「core instantiate 读 aria_controls 填 panel_ids」改成「RoleInfo.aria_controls（runtime 存储，instantiate 从 TemplateNode 拷入），sync_control_visuals 每帧解析」。

- [ ] **Step 2: roadmap §4 tech-debt aria-selected 条改写**

把「`[aria-selected]` state-attr selector + Node attrs 存储 + role dispatch」三条改写为：attr_matches_node role/aria/data-slot 支持 + 控件 role 分派**已在控件束 P1-P3 完成**（commit 0a7373d / a797840）；M3 真实落地 = TabList 复合控件（NodeKind+ControlState+synth aria-selected 跨节点+panel display+click/键盘+SelectionChanged），见 spec 2026-08-04。

- [ ] **Step 3: milestones M3 标 DONE + 修 exit criteria**

删过期条（attr_matches_node 扩展、role dispatch）；保留 TabList/Tab 投影 + `[aria-selected]` 命中；标 M3 状态（编码端 DONE，Unity 验收 defer 家里机）。

- [ ] **Step 4: settings.html 解注释（settings.html:16-17）**

```html
/* TODO(roadmap §4 tech-debt aria-selected): state-attr selector + tab 控件 defer to 控件束 TabList. */
/* .tab[aria-selected="true"] { background-color:rgba(26,47,69,0.85); color:#5fb4d4; font-weight:700; } */
```
改成（去 TODO 注释 + 解注释规则）：
```html
.tab[aria-selected="true"] { background-color:rgba(26,47,69,0.85); color:#5fb4d4; font-weight:700; }
```

- [ ] **Step 5: 重打 showcase pkg（v29）**

```bash
cargo run -p loomgui_pkg -- build showcase
```
确认 `showcase.pkg.bin` 头 version=29（PowerShell 读字节 4-8 = 29）。

- [ ] **Step 6: 重打所有 fixture pkg（v29）**

```bash
for ws in crates/packer/pkg/tests/fixtures/*.workspace; do cargo run -p loomgui_pkg -- build "$ws"; done
for ws in tests/dotnet/LoomGUI.HeadlessTests/fixtures/*.workspace; do cargo run -p loomgui_pkg -- build "$ws"; done
```

- [ ] **Step 7: 重编 GUI exe（绑 fence，坑 158）**

```bash
(cd crates/packer/gui/src-tauri && tauri build --no-bundle)
cp crates/packer/gui/src-tauri/target/release/loomgui_gui.exe unity/package/Editor/Tools/loomgui_gui.exe
```

- [ ] **Step 8: 全门绿**

```bash
cargo fmt --all -- --check
cargo clippy --all-targets -- -D warnings
cargo test
dotnet test tests/dotnet/LoomGUI.HeadlessTests/
dotnet build tests/dotnet/LoomGUI.PublicApi/LoomGUI.PublicApi.csproj
```
Expected: 全绿。fixture/pkg v29 一致。

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "feat(tablist): M3 complete — docs align + settings showcase + repack v29"
```

- [ ] **Step 10: Unity 真机验收（家里机，defer 出本计划）**

PlayMode settings 页：① 点 tab → 高亮（`[aria-selected="true"]` CSS 命中）+ 对应 panel 显示、其他隐藏；② 方向键切 tab；③ `SelectionChanged` typed 事件接到。记到 milestones M0/M3 验收清单。

---

## 风险与缓解（实现期参考 spec §10）

- **漏 dispatch arm**：Task 3/4 的 grep 步骤是硬要求。AGENTS.md 教训：NumberField 当年 enum/FFI/C# 各 task 都绿，render/measure arm 漏了 → 控件空 mesh。TabList/Tab 新增后强制 grep `NodeKind::Template` / `ControlState::Dropdown` 所有出现处逐一加。
- **kind_tag 取 button 还是 div**（Task 3 Step 6）：取决于 showcase 是否靠 `tab` tag 选择器。settings.html 不靠，默认 button（`<button role=tab>` 来源）。若别的页用 `<div role=tab>` 又靠 tag 选，再调。
- **panel id_index / find_node_by_id**（Task 6）：用现有全局 find_node_by_id（首匹配）。panel id 在 showcase 唯一，首匹配够。完整 IsScopeRoot 作用域查找是复合束的事，本计划不依赖。
- **aria-controls 解析失败**（R1）：tab 写了 aria-controls 但 panel 不存在 → sync_control_visuals 跳过该 tab（不 panic）。fence 期已校验 idref 存在，运行时动态缺则静默跳。
- **clone_node_recursive**（R5）：list item 模板内含 tablist 时，RoleTable 已克隆（commit 582ba8c），ControlState 克隆/re-init 核实（dynamic.rs:311 警告）。showcase 不触发则 defer。

## 不做的事（显式 defer，见 spec §11）

- Tree 复合控件（P4 第二棒）、roving tabindex/Home/End/Tab 焦点链、通用 Node attrs 仓库（β）、`aria-labelledby`、manual activation 键盘模型、radial/conic/多层 background 等视觉束项。
