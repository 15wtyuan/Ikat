# SDF 字体渲染 — Plan 1：Base SDF 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把核心自绘字体从 bitmap coverage AA 切到单一 SDF（TMP 风格），用 `_FaceDilate` 解决"字细"，任意缩放锐利。

**Architecture:** 自写 8SSEDT（Felzenszwalb 二维 EDT）把字形 coverage 转成 signed distance field，固定 SOURCE_SIZE 光栅存进 R8 atlas；`GlyphKey` 简化为 `(font_id, glyph_id)`（所有 size 共享一份 SDF）；`build_text_mesh` 的 quad 按 `target/SOURCE` 缩放；shader ALPHA_MASK 改采 distance + `ddx/ddy` 屏幕空间 scale + `smoothstep` + `_FaceDilate`。

**Tech Stack:** Rust（loomgui_core，edition 2021）、ab_glyph_rasterizer 0.1（出 coverage）、etagere 0.3（atlas 货架）、ttf-parser 0.20（outline/metrics）、Unity HLSL（URP，LoomGUI-Unlit.shader）。

## Global Constraints

（每个 task 的需求隐含以下约束，源自 spec `docs/superpowers/specs/2026-07-11-sdf-font-rendering-design.md` + CLAUDE.md）

- **常量**：`SOURCE_SIZE = 48`、`SPREAD = 12`（atlas.rs 顶部 const，单位 design px）。
- **依赖钉版本**：`ab_glyph_rasterizer = "0.1"`、`etagere = "0.3"`、`ttf-parser = "0.20"`，不改版本。
- **Rust edition 2021**；FFI 入口绝不 panic（atlas `ensure` 容错早返空帧，遇 None/越界不 unwrap/expect）。
- **注释**：中文、说 WHY 不复述机制、不引用内部编号或暗语（"坑 N"、"对齐某 meta" 禁止）；代码/commit 英文。
- **改 Rust 后必须重编 + commit `.dll`**：`cargo build -p loomgui_ffi_c --release` → 拷贝到 `loomgui_unity_package/Plugins/LoomGUI/loomgui_ffi_c.dll`（Unity 关着）。Plan 1 最后一个 task 统一做。
- **测试门**：每 task 后 `cargo test -p loomgui_core` 全绿；改 parse/围栏后跑 `cargo test -p loomgui_core --test fence_contract`。snapshot 用 `INSTA_UPDATE=always cargo test -p loomgui_core --test snapshot` 更新。
- **fmt/clippy**：push 前 `cargo fmt --all -- --check` + `cargo clippy --all-targets -- -D warnings` 必过。
- **scope 边界**：本 plan **只做 base SDF**。effect（shadow/stroke/glow/blur）在 Task 3 移除 bitmap 路径后**暂降级**（DSL 解析保留、渲染忽略、`page_text` C1/C2 known regression），由 Plan 2 搬 shader 恢复。渐变字（`background-clip:text`）+ 装饰线（underline/strike）不受影响，保留。

## File Structure

- **Create** `loomgui_core/src/text/sdf.rs` — 8SSEDT 算法（纯函数：二值 mask → signed distance；+ distance↔u8 编码）。
- **Modify** `loomgui_core/src/text/atlas.rs` — `rasterize_glyph` 改 SDF 输出；`GlyphKey` 简化；`GLYPH_PAD` → `SPREAD`；移除 effect 后处理路径（`register_effect`/`effects` 表）。
- **Modify** `loomgui_core/src/text/mod.rs` — `pub mod sdf;`（注册新模块）。
- **Modify** `loomgui_core/src/render/mod.rs` — `build_text_mesh`：quad 按 target/SOURCE 缩放；移除 shadow/glow/stroke/blur layer mesh 产出（effect 降级）。
- **Modify** `loomgui_unity_package/Shaders/LoomGUI-Unlit.shader` — ALPHA_MASK fragment 改 SDF 重建 + `_FaceDilate`。
- **Modify** `loomgui_core/src/text/font_effect.rs` — Plan 1 末加 `#[cfg]` 或 `#[allow(dead_code)]` 保 `FontEffect` 枚举与 `effect_sig`（Plan 2 用），删除已无 caller 的 `apply_effect`/`dilate`/`erode`/`gaussian_blur`（Task 3 评估）。

---

### Task 1: 8SSEDT 算法（`sdf.rs`）

8SSEDT（signed distance field）核心：二值 mask → 每像素到字形边缘的 signed distance（inside 正、outside 负）。纯函数 + 编码工具，本 task **不接入 atlas**。

**Files:**
- Create: `loomgui_core/src/text/sdf.rs`
- Modify: `loomgui_core/src/text/mod.rs`（加 `pub mod sdf;`）
- Test: `loomgui_core/src/text/sdf.rs` 内 `#[cfg(test)] mod tests`

**Interfaces:**
- Produces: `pub fn signed_distance_field(mask: &[u8], w: u32, h: u32) -> Vec<f32>`（mask：inside=1/outside=0；返回每像素 signed distance，像素单位，inside 正/outside 负）；`pub fn encode_distance(d: f32, spread: u32) -> u8`（d → `0.5 + d/(2*spread)` 钳 [0,1] ×255）；`pub fn decode_distance(u: u8, spread: u32) -> f32`（往返）。

- [ ] **Step 1: 写失败测试**

`loomgui_core/src/text/sdf.rs`：

```rust
//! 8SSEDT（8-point signed sequential Euclidean distance transform）：二值字形 mask
//! → 每像素到边缘的 signed distance（inside 正 / outside 负）。
//! 算法 = Felzenszwalb & Huttenlocher 二维 EDT（下包络线 1D EDT，先行再列两遍）。
//! signed = sqrt(edt_seed_outside) - sqrt(edt_seed_inside)：
//!   inside 像素 edt_inside=0 → signed = +到最近 outside 的距离；
//!   outside 像素 edt_outside=0 → signed = -到最近 inside 的距离。

/// 二值 mask（inside=1/outside=0）→ 每像素 signed distance（像素单位）。
/// mask 长度须 == (w*h)；越界/空返回空 Vec（FFI 邻近不 panic）。
pub fn signed_distance_field(mask: &[u8], w: u32, h: u32) -> Vec<f32> {
    let n = (w as usize) * (h as usize);
    if w == 0 || h == 0 || mask.len() != n {
        return Vec::new();
    }
    let w = w as usize;
    let h = h as usize;
    // 两个 seed grid：outside-seed（求 inside 像素到边缘）、inside-seed（求 outside 像素到边缘）。
    let mut f_outside = vec![f32::INFINITY; n]; // seed = inside 像素（0）
    let mut f_inside = vec![f32::INFINITY; n]; //  seed = outside 像素（0）
    for i in 0..n {
        if mask[i] != 0 {
            f_outside[i] = 0.0; // inside 是 outside-distance 的 seed
        } else {
            f_inside[i] = 0.0; // outside 是 inside-distance 的 seed
        }
    }
    edt_2d(&mut f_outside, w, h);
    edt_2d(&mut f_inside, w, h);
    let mut out = vec![0.0f32; n];
    for i in 0..n {
        let d_out = f_outside[i].max(0.0).sqrt(); // 到最近 inside 的距离
        let d_in = f_inside[i].max(0.0).sqrt(); //  到最近 outside 的距离
        // signed：inside 像素（mask=1）→ +d_in（到边缘外侧）；outside → -d_out。
        out[i] = if mask[i] != 0 { d_in } else { -d_out };
    }
    out
}

/// Felzenszwalb 1D EDT：f[i] = 该点已知的平方距离（seed=0，余 INF）→ out[i] = min_j(f[j]+(i-j)²)。
fn edt_1d(f: &[f32], out: &mut [f32]) {
    let n = f.len();
    if n == 0 {
        return;
    }
    let mut v = vec![0usize; n]; // 下包络顶点索引
    let mut z = vec![f32::NEG_INFINITY; n + 1]; // 拐点
    z[0] = f32::NEG_INFINITY;
    z[1] = f32::INFINITY;
    let mut k = 0usize;
    for q in 1..n {
        if f[q].is_infinite() {
            continue; // 非 seed 不参与包络
        }
        // 求 parabola(q) 与当前包络交点，弹掉被完全覆盖的旧顶点。
        let mut s = intersec(f, v[k], q);
        while s <= z[k] {
            if k == 0 {
                break;
            }
            k -= 1;
            s = intersec(f, v[k], q);
        }
        k += 1;
        v[k] = q;
        z[k] = s;
        z[k + 1] = f32::INFINITY;
    }
    let mut k = 0usize;
    for q in 0..n {
        while z[k + 1] < q as f32 {
            k += 1;
        }
        let dd = q as f32 - v[k] as f32;
        out[q] = dd * dd + f[v[k]];
    }
}

/// 两 parabola 交点横坐标（seed r 与 seed q）。
fn intersec(f: &[f32], r: usize, q: usize) -> f32 {
    let fr = f[r];
    let fq = f[q];
    ((fq + (q * q) as f32) - (fr + (r * r) as f32)) / (2.0 * (q - r) as f32)
}

/// 二维 EDT：先每行 1D，再每列 1D（对行结果）。结果为平方距离。
fn edt_2d(f: &mut [f32], w: usize, h: usize) {
    // 行 pass。
    let mut tmp = vec![0.0f32; w];
    for y in 0..h {
        let row = &mut f[y * w..y * w + w];
        edt_1d(row, &mut tmp);
        row.copy_from_slice(&tmp);
    }
    // 列 pass（对行结果再变换）。
    let mut col_in = vec![0.0f32; h];
    let mut col_out = vec![0.0f32; h];
    for x in 0..w {
        for y in 0..h {
            col_in[y] = f[y * w + x];
        }
        edt_1d(&col_in, &mut col_out);
        for y in 0..h {
            f[y * w + x] = col_out[y];
        }
    }
}

/// signed distance（像素）→ R8 编码：中心 0.5，inside>0.5、outside<0.5。
/// 超出 ±spread 的 distance 饱和到 0/255（spread 之外 effect 会被 clip，SDF 硬约束）。
pub fn encode_distance(d: f32, spread: u32) -> u8 {
    let spread = spread.max(1) as f32;
    let normalized = 0.5 + d / (2.0 * spread);
    (normalized.clamp(0.0, 1.0) * 255.0).round() as u8
}

/// R8 → signed distance（像素），encode_distance 的逆。
pub fn decode_distance(u: u8, spread: u32) -> f32 {
    let spread = spread.max(1) as f32;
    (u as f32 / 255.0 - 0.5) * 2.0 * spread
}

#[cfg(test)]
mod tests {
    use super::*;

    /// 实心矩形 mask（去掉 1px 边框）→ 内部距边缘应全部 >0。
    #[test]
    fn inside_pixels_positive_outside_negative() {
        // 5×5，中心 3×3 实心 inside（去掉外圈）。
        let mut mask = vec![0u8; 25];
        for y in 1..4 {
            for x in 1..4 {
                mask[y * 5 + x] = 1;
            }
        }
        let sdf = signed_distance_field(&mask, 5, 5);
        // 中心 (2,2) 在 inside，距最近 outside=2px → signed≈+2。
        assert!(sdf[2 * 5 + 2] > 1.5, "中心 inside 距离应 ≈2");
        // 角 (0,0) 在 outside，距最近 inside≈1px → signed≈-1.4。
        assert!(sdf[0] < -0.5, "角 outside 距离应为负");
    }

    /// 边缘 zero-crossing：跨越字形轮廓时 signed 从正变负。
    #[test]
    fn edge_zero_crossing() {
        // 左半 inside、右半 outside（竖直边缘在 x=2.5）。
        let mut mask = vec![0u8; 10]; // 5×2
        for y in 0..2 {
            for x in 0..2 {
                mask[y * 5 + x] = 1;
            }
        }
        let sdf = signed_distance_field(&mask, 5, 2);
        // x=1（inside 边缘）正，x=2（紧贴边缘 outside）负或小正。
        assert!(sdf[1] >= 0.0, "inside 边缘非负");
        assert!(sdf[3] <= 0.0, "outside 为负");
    }

    /// encode/decode 往返误差 < 1px/spread。
    #[test]
    fn encode_decode_roundtrip() {
        let spread = 12u32;
        for d in [-11.5f32, -6.0, -1.0, 0.0, 0.5, 5.0, 11.5] {
            let enc = encode_distance(d, spread);
            let dec = decode_distance(enc, spread);
            assert!(
                (dec - d).abs() < 1.0,
                "d={d} 往返 dec={dec} 误差过大"
            );
        }
        // 超 spread 饱和：encode 钳到 [0,255]，decode 回到 ∓spread。
        assert_eq!(encode_distance(20.0, 12), 255);
        assert_eq!(encode_distance(-20.0, 12), 0);
    }

    /// 空输入不 panic（FFI 邻近容错）。
    #[test]
    fn empty_input_no_panic() {
        let s = signed_distance_field(&[], 0, 0);
        assert!(s.is_empty());
    }
}
```

`loomgui_core/src/text/mod.rs` 加 `pub mod sdf;`（按字母序插进现有 `pub mod` 行）。

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p loomgui_core text::sdf`
Expected: 编译失败（`sdf.rs` 不存在 / `pub mod sdf` 找不到文件）。先建文件再跑，应 4 个测试全 PASS（算法实现已随文件给出）。若任一失败，修算法至全绿。

- [ ] **Step 3: 跑测试确认通过**

Run: `cargo test -p loomgui_core text::sdf`
Expected: 4 passed。

- [ ] **Step 4: clippy/fmt**

Run: `cargo clippy -p loomgui_core --all-targets -- -D warnings` 然后 `cargo fmt --all -- --check`
Expected: 无 warning、无 diff。修到干净。

- [ ] **Step 5: Commit**

```bash
git add loomgui_core/src/text/sdf.rs loomgui_core/src/text/mod.rs
git commit -m "feat(text): add 8SSEDT signed distance field algorithm"
```

---

### Task 2: `rasterize_glyph` 改 SDF 输出（`atlas.rs`）

把 `rasterize_glyph` 的输出从 coverage×255 改成 SDF distance 编码。固定 `SOURCE_SIZE` 光栅（不再按 target size）；bitmap padding 从 `GLYPH_PAD=1` 扩到 `SPREAD`。

**Files:**
- Modify: `loomgui_core/src/text/atlas.rs`（顶部加常量；`rasterize_glyph` 改）
- Test: `atlas.rs` 内 `#[cfg(test)]`

**Interfaces:**
- Consumes: Task 1 的 `sdf::signed_distance_field` / `sdf::encode_distance`。
- Produces: `rasterize_glyph` 仍返 `(Vec<u8>, u32, u32)`，但像素语义 coverage → distance（中心 128、边缘渐变）。
- 新常量 `pub const SOURCE_SIZE: u16 = 48;`、`pub const SPREAD: i32 = 12;`。`GLYPH_PAD` 改为 `= SPREAD`（保留名字以减小 build_text_mesh 改动面，Task 4 再用）。

- [ ] **Step 1: 改常量 + 写失败测试**

在 `atlas.rs` 顶部（`PAGE_SIZE` 附近）加：

```rust
/// SDF source 光栅尺寸（design px）。所有 target size 共享一份此尺寸的 SDF，
/// 运行时 quad 按 target/SOURCE_SIZE 缩放。TMP 中档值，CJK 笔画清晰、放大 72+ 仍锐利。
pub const SOURCE_SIZE: u16 = 48;
/// SDF spread：atlas 里每像素存 ±SPREAD 范围的 signed distance。
/// SDF 硬约束 = spread 须 ≥ 最大 effect 宽度（spread 之外 distance 饱和、effect 硬切）。
/// 12 覆盖 showcase text-shadow blur 12px。字形 bitmap 四周各扩 SPREAD px。
pub const SPREAD: i32 = 12;
```

把 `pub const GLYPH_PAD: i32 = 1;` 改为 `pub const GLYPH_PAD: i32 = SPREAD;`（语义：bitmap padding 从 1px 抗锯齿余量 → SDF spread 区域；名字保留，build_text_mesh 仍读它）。

在 `atlas.rs` 测试模块加：

```rust
    /// SDF 光栅：输出像素语义是 distance（中心≈128、字形内 >128、外 <128），非 coverage。
    #[test]
    fn rasterize_glyph_outputs_sdf_distance() {
        let mut a = GlyphAtlas::new();
        let f = dejavu_face();
        let gid = f.glyph_index('A').unwrap();
        let r = a.ensure(
            &f,
            GlyphKey {
                font_id: 0,
                glyph_id: gid.0,
                size_px: 0,
                effect_sig: 0,
            },
        );
        assert!(r.px_w > 0 && r.px_h > 0, "字形有轮廓");
        let (bytes, w, h) = a.page_bytes(0);
        // 找一个 inside 像素（>128）和一个 outside 像素（<128）证明 distance 语义。
        // 字形必然产生 >128（字形内）与 <128（字形外）两种值。
        let has_inside = bytes.iter().any(|&b| b > 140);
        let has_outside = bytes.iter().any(|&b| b < 100);
        assert!(has_inside, "SDF 应有 inside 像素（>140）");
        assert!(has_outside, "SDF 应有 outside 像素（<100）");
        let _ = (w, h);
    }
```

（注意：此测试用旧 `GlyphKey` 字段 `size_px/effect_sig`，Task 3 会简化。本 task 暂保留旧字段。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p loomgui_core rasterize_glyph_outputs_sdf_distance`
Expected: FAIL —— 当前 `rasterize_glyph` 输出 coverage（字形内≈255 单一值，无 <100 的 outside 渐变带），`has_outside` 断言可能过但 `has_inside` 阈值语义错；或像素分布不符合 distance。确认失败再改。

- [ ] **Step 3: 改 `rasterize_glyph` 实现**

把 `rasterize_glyph`（当前 line 282-328）的 coverage 写出段改为 SDF。关键改动：光栅 scale 固定 `SOURCE_SIZE`（不再用入参 `size_px`）；coverage → 二值 mask → `signed_distance_field` → `encode_distance`。

替换 `rasterize_glyph` 函数体中 bitmap 尺寸计算与 coverage 写出段：

```rust
fn rasterize_glyph(face: &Face<'_>, gid: u16, _size_px: u16) -> (Vec<u8>, u32, u32) {
    // SDF：固定 SOURCE_SIZE 光栅（入参 size_px 不再用，保留签名待 Task 3 随 GlyphKey 一起删）。
    let size_px = SOURCE_SIZE;
    let units = face.units_per_em();
    if units == 0 || size_px == 0 {
        return empty_or_tofu(gid, size_px);
    }
    let scale = size_px as f32 / units as f32;
    let bbox = match face.glyph_bounding_box(GlyphId(gid)) {
        Some(b) => b,
        None => return empty_or_tofu(gid, size_px),
    };
    // bitmap = bbox(source) + 2*SPREAD（SPREAD 是 SDF spread，也是 atlas padding）。
    let gw = ((bbox.x_max - bbox.x_min) as f32 * scale).ceil() as i32 + 2 * GLYPH_PAD;
    let gh = ((bbox.y_max - bbox.y_min) as f32 * scale).ceil() as i32 + 2 * GLYPH_PAD;
    if gw <= 0 || gh <= 0 || gw > PAGE_SIZE || gh > PAGE_SIZE {
        return empty_or_tofu(gid, size_px);
    }

    // 先光栅出 coverage（复用 ab_glyph Rasterizer + OutlineToRaster）。
    let mut raster = Rasterizer::new(gw as usize, gh as usize);
    let mut builder = OutlineToRaster {
        raster: &mut raster,
        scale,
        origin_x: bbox.x_min as f32,
        origin_y: bbox.y_max as f32,
        offset_px: GLYPH_PAD as f32,
        pen: Point::default(),
        start: Point::default(),
        has_contour: false,
        empty: true,
    };
    let _ = face.outline_glyph(GlyphId(gid), &mut builder);
    if builder.empty {
        return empty_or_tofu(gid, size_px);
    }
    let mut cov = vec![0u8; (gw * gh) as usize];
    raster.for_each_pixel_2d(|x, y, c| {
        let i = (y as usize) * (gw as usize) + (x as usize);
        cov[i] = if c > 0.5 { 1 } else { 0 }; // 二值化 threshold 0.5
    });
    // coverage 二值 mask → signed distance → R8 编码。
    let sdf = crate::text::sdf::signed_distance_field(&cov, gw as u32, gh as u32);
    let px: Vec<u8> = sdf
        .iter()
        .map(|&d| crate::text::sdf::encode_distance(d, SPREAD as u32))
        .collect();
    (px, gw as u32, gh as u32)
}
```

（`empty_or_tofu` / `tofu_box` 保留原样——tofu 框是矩形线，coverage 语义即可，不参与 SDF 重建；Plan 1 不改。若 tofu 也想 SDF 化留给后续，不阻塞。）

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p loomgui_core rasterize_glyph_outputs_sdf_distance`
Expected: PASS。可能波及其它 atlas 测试（如 `page_bytes_nonempty_after_allocate` 只验字节数=w*h，仍过；`different_size_different_slot` 验不同 size 不同槽——但 SDF 后 size 不再进光栅，size_px=32 vs 48 产出同 bitmap → 该测试会 FAIL，预期内，Task 3 改）。

- [ ] **Step 5: 临时修被波及的测试**

`different_size_different_slot`（atlas.rs:477）当前断言不同 size 不同槽，但 SDF 后 size 不进光栅。**临时**把它改为 `#[ignore]` 并加注释 `// Task 3 会随 GlyphKey 去 size_px 重写`，或改为断言"size 不再影响光栅"（同 bitmap）。本 step 选后者更诚实：

```rust
    #[test]
    fn different_size_no_longer_shards_atlas() {
        // SDF：单一字形槽，size 不进 key（Task 3 正式去 size_px；本 task 已固定 SOURCE 光栅）。
        let mut a = GlyphAtlas::new();
        let f = dejavu_face();
        let gid = f.glyph_index('A').unwrap();
        let r32 = a.ensure(&f, GlyphKey { font_id: 0, glyph_id: gid.0, size_px: 32, effect_sig: 0 });
        let r48 = a.ensure(&f, GlyphKey { font_id: 0, glyph_id: gid.0, size_px: 48, effect_sig: 0 });
        // size_px 仍进 key（Task 3 才删）→ 仍分槽，但 bitmap 内容相同（同 SOURCE 光栅）。
        assert_eq!((r32.px_w, r32.px_h), (r48.px_w, r48.px_h), "SDF 固定 SOURCE 光栅，bitmap 维度不随 size 变");
    }
```

- [ ] **Step 6: clippy/fmt + Commit**

Run: `cargo clippy -p loomgui_core --all-targets -- -D warnings && cargo fmt --all -- --check`
```bash
git add loomgui_core/src/text/atlas.rs
git commit -m "feat(text): rasterize_glyph outputs SDF distance (fixed SOURCE_SIZE)"
```

---

### Task 3: `GlyphKey` 简化 + effect layer 移除

`GlyphKey (font_id, glyph_id, size_px, effect_sig)` → `(font_id, glyph_id)`。单一 SDF（去 size_px）+ effect 降级（去 effect_sig，移除 bitmap 后处理与 shadow/glow/stroke/blur layer mesh 产出）。`page_text` C1/C2 effect 文字本 plan 暂不渲染（known regression，Plan 2 恢复）。

**Files:**
- Modify: `loomgui_core/src/text/atlas.rs`（`GlyphKey` struct；`ensure` 去 effect 分支；删 `register_effect`/`effects` 表；`ensure_solid` sentinel 适配）
- Modify: `loomgui_core/src/render/mod.rs`（`build_text_mesh`：base_key 去 size_px/effect_sig；删 shadow/glow/stroke/blur `_patterns`/`_pages` 与 layer 产出循环；`text_effects` 参数标 `_`/保留待 Plan 2）
- Modify: `loomgui_core/src/text/font_effect.rs`（评估：删 `apply_effect`/`dilate`/`erode`/`gaussian_blur`/`convolve`/`stroke_ring`/`pad_bitmap`/`blur_pad` 已无 caller 的函数；保留 `FontEffect` 枚举 + `effect_sig` 供 Plan 2；删对应测试）
- Test: 上述三文件的 `#[cfg(test)]`

**Interfaces:**
- Produces: `GlyphKey { font_id: u32, glyph_id: u16 }`；`build_text_mesh` 不再产 shadow/stroke/glow/blur layer（`TextMeshes` 只含 base + 渐变字 + 装饰线）。

- [ ] **Step 1: 改 `GlyphKey` struct（写失败测试先）**

在 `atlas.rs` 测试模块加：

```rust
    #[test]
    fn glyph_key_no_size_no_effect() {
        // 单一 SDF：GlyphKey 只 (font_id, glyph_id)。同字形不同 size 返同 UV（共享 SDF 槽）。
        let mut a = GlyphAtlas::new();
        let f = dejavu_face();
        let gid = f.glyph_index('A').unwrap();
        // 注：Task 3 后 GlyphKey 无 size_px 字段——构造只传 font_id+glyph_id。
        let r1 = a.ensure(&f, GlyphKey { font_id: 0, glyph_id: gid.0 });
        let r2 = a.ensure(&f, GlyphKey { font_id: 0, glyph_id: gid.0 });
        assert_eq!((r1.u0, r1.v0, r1.u1, r1.v1), (r2.u0, r2.v0, r2.u1, r2.v1));
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p loomgui_core glyph_key_no_size_no_effect`
Expected: 编译失败（`GlyphKey` 还有 `size_px`/`effect_sig` 字段，构造少传字段报错）。

- [ ] **Step 3: 简化 `GlyphKey` + `ensure` + `ensure_solid`**

`atlas.rs`：

```rust
/// 字形缓存键。单一 SDF：所有 size 共享一份 source SDF，size 不进 key。
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct GlyphKey {
    pub font_id: u32,
    pub glyph_id: u16,
}
```

`ensure` 改：删 `key.effect_sig != 0` 的 effect 后处理整块（原 line 134-153），直接 `rasterize_glyph` → blit。`rasterize_glyph(face, key.glyph_id, 0)`（第 3 参已无用，传 0；签名下 step 统一删）。

删 `GlyphAtlas` 的 `effects: HashMap<u64, FontEffect>` 字段 + `register_effect` 方法 + `Default::default`/`new` 里对它的初始化。

`ensure_solid` sentinel key 改为 `GlyphKey { font_id: u32::MAX, glyph_id: u16::MAX }`（去 size_px/effect_sig）。

把 `rasterize_glyph` 签名从 `(face, gid, _size_px)` 改为 `(face, gid)`，删 `_size_px` 参，`ensure` 调用点同步改。

- [ ] **Step 4: 改 `build_text_mesh`（render/mod.rs）—— 移除 effect layer**

`base_key`（原 line 1318）改为 `let base_key = GlyphKey { font_id: g.font_id, glyph_id: g.glyph_id };`。

删 `shadow_patterns`/`glow_patterns`/`stroke_patterns`/`blur_patterns` 的构建（原 line 1222-1290，4 个 `filter_map` 块）+ 对应 `shadow_pages`/`glow_pages`/`stroke_pages`/`blur_pages` 声明 + 4 个 layer 产出循环（原 line 1385-1537）。

`build_text_mesh` 的 `text_effects: &[FontEffect]` 参数保留（Plan 2 接回 uniform 打包），但本 plan 未用 → 前缀改 `_text_effects` 或加 `let _ = text_effects;` 抑制 unused 警告；并更新 doc 注释说明"Plan 2 搬 shader"。

`TextMeshes` 返回值只填 base（+ 渐变字 vertex color + 装饰线，这两段不动）。

- [ ] **Step 5: 清理 `font_effect.rs` dead code**

删 `apply_effect`/`dilate`/`erode`/`convolve`/`stroke_ring`/`pad_bitmap`/`blur_pad`/`gaussian_blur`（已无 caller）+ 它们的测试（`gaussian_blur_*`/`dilate_*`/`erode_*`/`apply_effect_*`/`stroke_*`/`blur_*`）。保留 `FontEffect` 枚举 + `effect_sig` + `hash_color` + 它们的测试（`effect_sig_*`，Plan 2 uniform 打包用）。

更新 `font_effect.rs` 顶部模块注释：从"挂在 atlas ensure 内做位图后处理"改为"FontEffect 配置 + effect_sig 指纹；位图后处理已废，Plan 2 改 shader uniform 打包"。

- [ ] **Step 6: 改被波及的 atlas 测试**

atlas.rs 测试里所有 `GlyphKey { ... size_px: ..., effect_sig: ... }` 构造去掉这两个字段（`ensure_then_hit_returns_same_uv`/`new_glyph_marks_page_dirty_until_clear`/`page_bytes_nonempty_after_allocate`/`overflow_allocates_second_page`/`ensure_applies_registered_effect`/`ensure_skips_effect_when_unregistered`/`ensure_effect_sig_zero_skips_postprocess`）。后三个 effect 相关测试整删（机制已废）。`different_size_no_longer_shards_atlas`（Task 2 加的）改为断言"size 不再进 key"——可删（GlyphKey 已无 size_px）或保留为"同字形同槽"。

- [ ] **Step 7: 跑全核心测试确认通过**

Run: `cargo test -p loomgui_core`
Expected: 全绿。`cargo test -p loomgui_core --test fence_contract` 也绿（不改围栏）。snapshot 测试可能因 atlas 像素变（coverage→distance）而 FAIL——预期，下一个 task 前用 `INSTA_UPDATE=always cargo test -p loomgui_core --test snapshot` 更新，review 新 snapshot 是 distance 渐变形态（非全黑/全白 coverage）。

- [ ] **Step 8: clippy/fmt + Commit**

Run: `cargo clippy --all-targets -- -D warnings && cargo fmt --all -- --check`
```bash
git add loomgui_core/src/text/atlas.rs loomgui_core/src/text/font_effect.rs loomgui_core/src/render/mod.rs
git commit -m "refactor(text): simplify GlyphKey to (font_id,glyph_id); drop bitmap effect path"
```

---

### Task 4: `build_text_mesh` quad 按 target/SOURCE 缩放

单一 SDF 存的是 source（48px）维度的 bitmap，渲染时 quad 要缩放到 target size。pad 从 `GLYPH_PAD`（=SPREAD，source 空间）按 scale 放大到 target。

**Files:**
- Modify: `loomgui_core/src/render/mod.rs`（`build_text_mesh` base quad 段，原 line 1338 左右）
- Test: `loomgui_core/src/render/tests.rs`

**Interfaces:**
- Consumes: `atlas::SOURCE_SIZE`、`atlas::GLYPH_PAD`（=SPREAD）。
- Produces: base quad = `r.px_w * scale` × `r.px_h * scale`，`scale = run.font_size / SOURCE_SIZE`。

- [ ] **Step 1: 写失败测试**

`render/tests.rs` 加（参照现有 `build_text_snaps_quad_to_integer_pixel` 的构造模式）：

```rust
    #[test]
    fn build_text_quad_scales_by_target_over_source() {
        // SDF：atlas 存 SOURCE_SIZE(48) 维度 bitmap，build 时 quad 按 target/SOURCE 缩放。
        // target=48（=SOURCE）→ quad 维度 = atlas bitmap 维度；
        // target=24（=SOURCE/2）→ quad 维度减半。
        let (atlas48, layout48, rect48) = build_text_fixture(48.0); // 自建 helper：font_size=48 的 layout
        let m48 = build_text_mesh(&layout48, &mut { atlas48 }, &fonts(), &rect48, &[], None, false);
        let (atlas24, layout24, rect24) = build_text_fixture(24.0);
        let m24 = build_text_mesh(&layout24, &mut { atlas24 }, &fonts(), &rect24, &[], None, false);
        // 取首个 base quad 宽度（两个顶点 x 差）。
        let w48 = quad_width(&m48);
        let w24 = quad_width(&m24);
        assert!((w48 - 2.0 * w24).abs() < 1.0, "target 减半 → quad 宽度减半，w48={w48} w24={w24}");
    }
```

（`build_text_fixture`/`quad_width`/`fonts` 是测试 helper——若 `render/tests.rs` 已有类似 fixture 复用；没有就本 test 内自建最小 layout：单字形 'A'、`GlyphRun.font_size=target`、`rect=Rect{x:0,y:0,w:0,h:0}`。`quad_width` 取 mesh 第一组的 verts[0].x 与 verts[1].x 差绝对值。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p loomgui_core build_text_quad_scales_by_target_over_source`
Expected: FAIL —— 当前 quad 用 `r.px_w`（source 维度）不缩放，w48 ≈ w24（同 SOURCE bitmap），`w48 - 2*w24` 远大于 1。

- [ ] **Step 3: 改 quad 公式**

`render/mod.rs` `build_text_mesh` base quad 段（原 line 1333-1347）。在 `for &boff in bold_offsets` 循环前算 scale，quad 用 scale：

```rust
                        // SDF：atlas bitmap 是 SOURCE_SIZE 维度，quad 按 target/SOURCE 缩放到目标字号。
                        let scale = run.font_size / crate::text::atlas::SOURCE_SIZE as f32;
                        let pad_scaled = pad * scale; // GLYPH_PAD(=SPREAD) 随 quad 缩放
                        for &boff in bold_offsets {
                            // pixel snap：原点 round 到整数 design px（sf=1 时 = 屏幕像素整数）。
                            // bearing 来自 layout（target size），不乘 scale；pad 是 source 空间，乘 scale。
                            let left = (g.x + g.bearing_x - pad_scaled + boff + rect.x).round();
                            let top = (line.baseline - g.bearing_y - pad_scaled + rect.y).round();
                            let right = left + r.px_w as f32 * scale;
                            let bottom = top + r.px_h as f32 * scale;
                            let skew_top = italic_skew * r.px_h as f32 * scale;
                            // ... 顶点序 BL,BR,TR,TL 不变，用上述 left/top/right/bottom/skew_top。
```

（其余 push 顶点/UV/index 代码不变。`pad` 变量原 = `GLYPH_PAD as f32`，现乘 scale 得 `pad_scaled`。）

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p loomgui_core build_text_quad_scales_by_target_over_source`
Expected: PASS。跑全 `render` 测试 + snapshot（`INSTA_UPDATE=always` 更新，quad 维度变了）。

- [ ] **Step 5: clippy/fmt + Commit**

Run: `cargo clippy --all-targets -- -D warnings && cargo fmt --all -- --check`
```bash
git add loomgui_core/src/render/mod.rs loomgui_core/src/render/tests.rs
git commit -m "feat(text): scale text quad by target/SOURCE_SIZE for single-SDF"
```

---

### Task 5: shader base SDF + `_FaceDilate`

`LoomGUI-Unlit.shader` ALPHA_MASK fragment 从 coverage 线性 alpha 改成 SDF `smoothstep` 重建 + `_FaceDilate` 调厚。新增 `_FaceDilate`/`_GradientScale` Properties（material 默认，不进 FFI）。

**Files:**
- Modify: `loomgui_unity_package/Shaders/LoomGUI-Unlit.shader`

**Interfaces:**
- Consumes: R8 atlas（`.r` = encoded distance，中心 128）。`_GradientScale` = SPREAD + 1（对标 TMP）。
- Produces: ALPHA_MASK fragment 用 `ddx/ddy` 算屏幕 scale + `smoothstep` 重建 face alpha + `_FaceDilate` 偏移 threshold。

- [ ] **Step 1: 加 Properties + 改 CBUFFER**

shader `Properties{}` 块加（在 `_CornerRadius` 后）：

```hlsl
        _FaceDilate("Face Dilate", Range(-1,1)) = 0.15   // 调厚治"字细"：正值向内推 threshold 增粗
        _GradientScale("Gradient Scale", Float) = 13     // = SPREAD(12)+1，distance→屏幕换算（对标 TMP _GradientScale=atlasPadding+1）
```

`CBUFFER_START(UnityPerMaterial)` 加：

```hlsl
                float _FaceDilate;
                float _GradientScale;
```

- [ ] **Step 2: 改 ALPHA_MASK fragment**

把 ALPHA_MASK 分支（原 line 102-105）从：

```hlsl
                #if defined(ALPHA_MASK)
                half4 col = half4(vcol.rgb, vcol.a * tex.r);
```

改为 SDF 重建：

```hlsl
                #if defined(ALPHA_MASK)
                // SDF：tex.r 是 encoded distance（中心 0.5、inside>0.5）。
                // 屏幕空间 scale：ddx/ddy 自适应屏幕像素密度，任意尺寸/缩放锐利（对标 TMP SSD.cginc:95）。
                float2 uvDx = ddx(i.uv);
                float2 uvDy = ddy(i.uv);
                float pxSize = rsqrt(abs(uvDx.x * uvDy.y - uvDx.y * uvDy.x));
                float scale = pxSize * (1.3333 * _GradientScale) / _MainTex_TexelSize.z;
                // raw 0-1 distance（中心 0.5）。scale 已含 _GradientScale 补偿编码斜率，
                // d 不再解码——解码会 double-count 斜率 → AA 带过锐（对标 TMP_SDF_SSD.cginc:92/105）。
                float d = tex.r;
                float threshold = 0.5 - _FaceDilate * 0.5;  // 正值 _FaceDilate 降阈值 → 增粗
                float faceAlpha = saturate((d - threshold) * scale + 0.5);
                half4 col = half4(vcol.rgb, vcol.a * faceAlpha);
```

（`_MainTex_TexelSize.z` = 1/atlasWidth，Unity 自动提供。`1.3333` = 4/3，TMP param.y 系数。d 用 raw 0-1（中心 0.5），scale 的 _GradientScale 已补偿 SDF 编码斜率——若再把 d 解码到像素会 double-count 斜率、AA 带过锐。对标 TMP_SDF_SSD.cginc:92/105。）

- [ ] **Step 3: 本地验证（无单测，靠编译 + Plan 1 末 PlayMode）**

shader 无 Rust 单测。本 step 验证：shader 语法（Unity 控制台无编译错）。Rust 侧 `cargo test -p loomgui_core` 仍全绿（shader 不影响）。

- [ ] **Step 4: Commit**

```bash
git add loomgui_unity_package/Shaders/LoomGUI-Unlit.shader
git commit -m "feat(shader): SDF reconstruction + _FaceDilate in ALPHA_MASK pass"
```

---

### Task 6: 重编 `.dll` + snapshot 更新 + 家里验收准备

Plan 1 收尾：重编 FFI .dll + 拷贝 + 更新 snapshot + 文档化 effect known regression。家里 PlayMode 验收 page_image 字细。

**Files:**
- Modify: `loomgui_unity_package/Plugins/LoomGUI/loomgui_ffi_c.dll`（重编拷贝）
- Modify: `loomgui_core/tests/snapshots/*`（INSTA_UPDATE 更新）
- Modify: `docs/superpowers/specs/2026-07-11-sdf-font-rendering-design.md`（若实施中发现 spec 与实际偏差，记进 spec 附录）

- [ ] **Step 1: 重编 .dll**

Run:
```bash
cargo build -p loomgui_ffi_c --release
cp target/release/loomgui_ffi_c.dll loomgui_unity_package/Plugins/LoomGUI/loomgui_ffi_c.dll
```
（**Unity 必须关着**，否则 .dll 被锁。）验证 `md5sum target/release/loomgui_ffi_c.dll loomgui_unity_package/Plugins/LoomGUI/loomgui_ffi_c.dll` 两文件 md5 一致（防 stale .dll）。

- [ ] **Step 2: 更新 snapshot**

Run: `INSTA_UPDATE=always cargo test -p loomgui_core --test snapshot`
review 新 snapshot：atlas 像素应是 distance 渐变（中心灰、边缘渐变），非 coverage（全黑/全白）。无意义的 diff（如 quad 顶点因 scale 变）确认是预期。

再 `cargo test -p loomgui_core --test snapshot` 确认全绿。

- [ ] **Step 3: fmt/clippy 全量门**

Run:
```bash
cargo fmt --all -- --check
cargo clippy --all-targets -- -D warnings
cargo test -p loomgui_core
cargo test -p loomgui_core --test fence_contract
```
Expected: 全绿。

- [ ] **Step 4: Commit .dll + snapshot**

```bash
git add loomgui_unity_package/Plugins/LoomGUI/loomgui_ffi_c.dll loomgui_core/tests/snapshots/
git commit -m "feat(text): rebuild dll + update snapshots for base SDF"
```

- [ ] **Step 5: 家里 PlayMode 验收（两机工作流）**

家里机拉代码 → Unity PlayMode 加载 showcase：
- ✅ **page_image 3.1/3.2/3.3**：字细改善，`_FaceDilate=0.15` 下视觉重量对齐 HTML 预览；3.3 transform scale/rotate 下文字锐利（SDF 重建）。
- ⚠️ **page_text C1/C2**：effect（shadow/stroke）暂不渲染（known regression，Plan 2 恢复）。渐变字（若有）+ 装饰线正常。
- 调参：若字仍偏细/偏粗，改 shader `_FaceDilate` 默认值（Properties 那行），重编不涉及 Rust（shader 改即时）。

验收通过 → Plan 1 完成。Plan 2（effect 搬 shader）恢复 C1/C2。

---

## Self-Review（写计划后自查）

**1. Spec 覆盖**：Plan 1 覆盖 spec §2.1（单一 SDF）、§2.2（8SSEDT）、§2.3（R8）、§2.5（SOURCE/SPREAD）、§3.2（光栅）、§3.3（GlyphKey）、§3.4（quad）、§3.5（shader base + _FaceDilate）。§2.4/§3.5-3.6（effect 搬 shader）+ §3.7（FFI effect 契约）属 Plan 2，本 plan 在 Task 3 明确 effect 降级为 known regression。§4 测试策略分散进各 task。✅

**2. Placeholder 扫描**：无 TBD/TODO；每 step 有具体代码或命令。shader task（Task 5）无单测是客观限制（shader 不可单测），靠 PlayMode 验收（Task 6 Step 5），已标注。✅

**3. 类型一致**：`GlyphKey` 在 Task 3 统一改为 `{font_id, glyph_id}`，后续 task（Task 4 不再构造 key，用 atlas.ensure 返回值）一致。`signed_distance_field`/`encode_distance`/`decode_distance` 签名 Task 1 定，Task 2 消费，一致。`SOURCE_SIZE`/`SPREAD` Task 2 定为 `pub const`，Task 4/5 消费，一致。✅
